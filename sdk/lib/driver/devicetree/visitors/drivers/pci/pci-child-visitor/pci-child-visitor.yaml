# Copyright 2026 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# See https://www.kernel.org/doc/Documentation/devicetree/bindings/pci/pci.txt
# for additional details.

%YAML 1.2
---
$id: http://devicetree.org/schemas/pci-child-visitor.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: PCI child visitor

description: |
  Visits nodes describing a PCI bus (those with `device_type = "pci"`) and wires
  each child to the PCI device that the PCI bus driver discovers at the child's
  bus/device/function (BDF) address.

maintainers:
  - Gurjant Kalsi (gkalsi@google.com)

properties:
  device_type:
    const: pci

  "#address-cells":
    const: 3

  "#size-cells":
    const: 2

  fuchsia,pci-domain:
    $ref: /schemas/types.yaml#/definitions/uint32
    description: |
      Optional PCI domain (segment group) that this host bridge and everything
      below it lives in. Buses nested below the host bridge (PCI-to-PCI
      bridges) inherit it and must not declare their own. Defaults to 0 when
      absent, so devicetrees describing a single PCI domain may omit it. Must
      fit in 16 bits, as it becomes the PCI segment group.
    maximum: 0xffff

patternProperties:
  "^.*@[0-9a-f]+(,[0-9a-f]+)?$":
    type: object
    properties:
      reg:
        description: |
          PCI address of the child device. The first cell ("phys.hi") encodes
          the bus/device/function, following the standard PCI devicetree
          binding.
      pci-id:
        $ref: /schemas/types.yaml#/definitions/uint32-array
        description: |
          Optional, Fuchsia-specific <vendor-id device-id> pair. When present,
          the child is given PCI vendor/device id bind properties so it can bind
          a driver by id rather than only by compatible string. This is not part
          of the standard PCI devicetree binding.
        minItems: 2
        maxItems: 2
    required:
      - reg

additionalProperties: true
