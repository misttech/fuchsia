This file marks the root of this Fuchsia checkout and allows `fx`, `ffx` and
`hermetic-env` from other fuchsia checkouts to redirect `$PATH`-invocations to
the `fx`, `ffx` and `hermetic-env` scripts from this fuchsia checkout.
