# Copyright 2025 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("//build/components.gni")
import("//build/rust/rustc_library.gni")

group("camera_watcher_agent") {
  testonly = true
  deps = [
    ":settings_camera_watcher_agent",
    ":tests",
  ]
}

rustc_library("settings_camera_watcher_agent") {
  name = "settings_camera_watcher_agent"
  with_unit_tests = true
  edition = "2021"

  visibility = [
    ":*",
    "//src/settings/input:*",
    "//src/settings/service:*",
  ]

  deps = [
    "//src/lib/fuchsia-async",
    "//src/lib/trace/rust:trace",
    "//src/settings/camera:settings_camera",
    "//src/settings/common:settings_common",
    "//third_party/rust_crates:anyhow",
    "//third_party/rust_crates:futures",
  ]

  test_deps = [
    "//src/lib/fuchsia",
    "//src/settings/test_common:settings_test_common",
  ]

  sources = [ "src/lib.rs" ]

  configs -= [ "//build/config/rust/lints:allow_unused_results" ]
  configs += [ "//build/config/rust/lints:clippy_warn_all" ]
}

fuchsia_unittest_package("settings_camera_watcher_agent_tests") {
  component_name = "settings_camera_watcher_agent_test"
  manifest = "meta/settings_camera_watcher_agent_tests.cml"
  deps = [ ":settings_camera_watcher_agent_test" ]
  test_specs = {
    log_settings = {
      max_severity = "ERROR"
    }
  }
}

group("tests") {
  testonly = true
  deps = [ ":settings_camera_watcher_agent_tests" ]
}
