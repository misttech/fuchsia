# Copyright 2025 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.
import("//build/python/python_mobly_test.gni")
import("//build/testing/host_test_data.gni")
import("//src/testing/end_to_end/antlion/antlion_host_test.gni")
import("./test_lists.gni")

assert(is_host, "WLAN end-to-end tests only supported for host testing")

antlion_host_test("wlan_canary_test") {
  main_source = "WlanCanaryTest.py"
  libraries = [
    "//src/connectivity/wlan/tests/wlanix/wlanix_testing",
    "//src/connectivity/wlan/wlanix:fuchsia.wlan.wlanix_python",
    "//sdk/fidl/fuchsia.wlan.device.service:fuchsia.wlan.device.service_python",
  ]
}

antlion_host_test("wifi_chip_existence_test") {
  main_source = "//src/connectivity/wlan/tests/wlanix/WifiChipExistenceTest.py"
  libraries = [
    "//src/connectivity/wlan/tests/wlanix/wlanix_testing",
    "//src/connectivity/wlan/wlanix:fuchsia.wlan.wlanix_python",
    "//sdk/fidl/fuchsia.wlan.device.service:fuchsia.wlan.device.service_python",
  ]
}

antlion_host_test("wifi_chip_restart_test") {
  main_source = "//src/connectivity/wlan/tests/wlanix/WifiChipRestartTest.py"
  libraries = [
    "//src/connectivity/wlan/tests/wlanix/wlanix_testing",
    "//src/connectivity/wlan/wlanix:fuchsia.wlan.wlanix_python",
    "//sdk/fidl/fuchsia.wlan.device.service:fuchsia.wlan.device.service_python",
  ]
}

antlion_host_test("wifi_chip_correctness_and_consistency_test") {
  main_source = "//src/connectivity/wlan/tests/wlanix/WifiChipCorrectnessAndConsistencyTest.py"
  libraries = [
    "//src/connectivity/wlan/tests/wlanix/wlanix_testing",
    "//src/connectivity/wlan/wlanix:fuchsia.wlan.wlanix_python",
    "//sdk/fidl/fuchsia.wlan.device.service:fuchsia.wlan.device.service_python",
  ]
}

antlion_host_test("iface_lifecycle_test") {
  main_source = "//src/connectivity/wlan/tests/wlanix/IfaceLifecycleTest.py"
  libraries = [
    "//src/connectivity/wlan/tests/wlanix/wlanix_testing",
    "//src/connectivity/wlan/wlanix:fuchsia.wlan.wlanix_python",
    "//sdk/fidl/fuchsia.wlan.device.service:fuchsia.wlan.device.service_python",
  ]
}

antlion_host_test("iface_correctness_and_consistency_test") {
  main_source = "//src/connectivity/wlan/tests/wlanix/IfaceCorrectnessAndConsistencyTest.py"
  libraries = [
    "//src/connectivity/wlan/tests/wlanix/wlanix_testing",
    "//src/connectivity/wlan/wlanix:fuchsia.wlan.wlanix_python",
    "//sdk/fidl/fuchsia.wlan.device.service:fuchsia.wlan.device.service_python",
  ]
}

antlion_host_test("connect_to_ap_test") {
  main_source = "//src/connectivity/wlan/tests/wlanix/ConnectToApTest.py"
  libraries = [
    "//src/connectivity/wlan/tests/wlanix/wlanix_testing",
    "//src/connectivity/wlan/wlanix:fuchsia.wlan.wlanix_python",
    "//sdk/fidl/fuchsia.wlan.device.service:fuchsia.wlan.device.service_python",
  ]
}

antlion_host_test("apf_test") {
  main_source = "//src/connectivity/wlan/tests/wlanix/ApfTest.py"
  libraries = [
    "//src/connectivity/wlan/tests/wlanix/wlanix_testing",
    "//src/connectivity/wlan/wlanix:fuchsia.wlan.wlanix_python",
    "//sdk/fidl/fuchsia.wlan.device.service:fuchsia.wlan.device.service_python",
  ]
}

antlion_host_test("sched_scan_test") {
  main_source = "//src/connectivity/wlan/tests/wlanix/SchedScanTest.py"
  libraries = [
    "//src/connectivity/wlan/tests/wlanix/wlanix_testing",
    "//src/connectivity/wlan/wlanix:fuchsia.wlan.wlanix_python",
    "//src/testing/end_to_end/mobly_base_tests/fuchsia_async_extension",
    "//sdk/fidl/fuchsia.wlan.device.service:fuchsia.wlan.device.service_python",
    "//src/connectivity/wlan/tests/common",
  ]
}

# TODO(https://fxbug.dev/490145504): Plumb this test through to a builder with performance capture enabled.
antlion_host_test("create_client_iface_benchmark_test") {
  main_source =
      "//src/connectivity/wlan/tests/wlanix/CreateClientIfaceBenchmarkTest.py"
  libraries = [
    "//src/connectivity/wlan/tests/wlanix/wlanix_testing",
    "//src/connectivity/wlan/wlanix:fuchsia.wlan.wlanix_python",
    "//sdk/fidl/fuchsia.wlan.device.service:fuchsia.wlan.device.service_python",
    "//src/performance/lib/trace_processing:trace_processing",
    "//src/performance/lib/perf_publish",
  ]
  data_sources = [
    "//src/tests/end_to_end/perf/expected_metric_names/fuchsia.wlan.wlanix.txt",
  ]
  data_package_name = "test_data"
}

# This is a helper group so developers can easily add all tests to their build.
# It is not run by infra. You should not directly add tests here, but rather to
# one of the other groups below so it runs in infra.
group("tests_host") {
  testonly = true
  public_deps = [
    ":tests_for_fyi",
    ":tests_for_fyi_long",
    ":tests_for_fyi_performance",
    ":tests_for_postsubmit",
    ":tests_for_presubmit",
    ":tests_for_presubmit_no_shield_box",
    ":tests_for_wlan_presubmit",
  ]
}

# These tests run as a blocking tryjob against every single CL anywhere
# in the tree. Therefore, we need to be extremely conservative with
# the tests we choose to run here, ensuring they give us sufficient
# signal for the amount of testbed load they'll require.
# Consider instead putting your test in `tests_for_wlan_presubmit`
group("tests_for_presubmit") {
  testonly = true
  public_deps = wlanix_tests_for_presubmit  # Edit in test_lists.gni
}

# Only tests which do not interact with a AP or SoftAP can be run
# without a shield box. These tests can run against any DUT in
# the infra pool of devices, which are much less capacity-limited
# than the WLAN-specific shielded testbeds.
group("tests_for_presubmit_no_shield_box") {
  testonly = true
  public_deps =
      wlanix_tests_for_presubmit_no_shield_box  # Edit in test_lists.gni
}

# These tests run in blocking CI (~once per hour) as well as a blocking
# tryjob on any CL with "wlan" in a file path. Tests which exercise WLAN
# code that changes frequently or tests that provide broad WLAN code coverage
# should go here, but most tests should likely go into `tests_for_postsubmit`.
group("tests_for_wlan_presubmit") {
  testonly = true
  public_deps = wlanix_tests_for_wlan_presubmit  # Edit in test_lists.gni
}

# These tests run in blocking CI (~once per hour).
group("tests_for_postsubmit") {
  testonly = true
  public_deps = wlanix_tests_for_postsubmit  # Edit in test_lists.gni
}

# If you're writing a new test, add it to FYI first until it has a sufficient
# number of passing runs (either here in FYI or via `fx test-remote`). Tests in
# FYI don't cause any alerting if they break, so having the test here doesn't
# provide much of a safety net. Consider moving it to one of the blocking groups
# above.
group("tests_for_fyi") {
  testonly = true
  public_deps = wlanix_tests_for_fyi  # Edit in test_lists.gni
}

# These tests don't cause alerting if they break, and are configured to
# upload performance data to fuchsiaperf via Catapult.
group("tests_for_fyi_performance") {
  testonly = true
  public_deps = wlanix_tests_for_fyi_performance  # Edit in test_lists.gni
}

# Tests longer than ~20 minutes consume significant resources and significantly
# lengthen the cadence of the test runs. Split these tests out so we can run
# them at reduced frequency.
group("tests_for_fyi_long") {
  testonly = true
  public_deps = wlanix_tests_for_fyi_long  # Edit in test_lists.gni
}
