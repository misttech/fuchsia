# Driver Serve FIDL Test

This directory contains a skeleton Rust driver and a test designed to verify
that the driver serves a FIDL service.

Currently, the driver does not serve any service, so the test is expected to
fail. This setup is used to evaluate an AI assistant's capability to implement
FIDL serving in a Rust driver.
