Verifies that the device suspends and resumes at the appropriate times.

### Running the test locally

There currently isn't a way to run this version of the test locally. However,
see the README.md in the directory in //vendor/google with
suspend_resume_test.py for how to run a similar test locally.