# Hidden unreachable file

This file does not appear in any `_toc.yaml` files. However, because its filename starts with an underscore `_`, `doc_checker` exempts it from the reachability check.

It contains a valid link to [README][readme] to ensure link checking passes.

[readme]: README.md
