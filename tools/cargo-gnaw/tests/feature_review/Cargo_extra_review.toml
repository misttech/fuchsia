[package]
name = "feature_test"
version = "0.1.0"
edition = "2024"

[dependencies]
needs_feature = { path = "needs_feature" }

[gn.package.crate_with_features."0.1.0"]
reviewed_features = ["feature1"]
