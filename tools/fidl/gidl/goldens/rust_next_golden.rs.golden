

#![cfg(test)]
#![allow(unused_imports)]
#![allow(invalid_from_utf8)]

use {
    assert_matches::assert_matches,
    fidl_next::{Chunk, WireU64,
        Decode, DecodeError, DecoderExt as _,
        Encode, EncodeError, EncoderExt as _,
        ValidationError,
        ClientEnd, ServerEnd},
    fidl_next::fuchsia::channel::Buffer,
    fidl_next::fuchsia::channel::RecvBuffer,
    fidl_next::fuchsia::zx as zx,
    fidl_next_test_conformance as test_conformance,
    zerocopy::IntoBytes,
    zx_status::Status,
    zx_types as zx_types,
    gidl_util::{
        HandleDef, HandleSubtype, copy_handle, create_handles, decode_value,
        get_handle_koid, get_info_handle_valid, repeat, select_handle_infos,
        select_raw_handle_infos, to_zx_handle_disposition_t,
    },
    std::convert::TryFrom,
    zx::{NullableHandle, AsHandleRef as _},
};



    #[test]
    fn encode_golden_bool_struct() {
        let value = test_conformance::GoldenBoolStruct { v: true };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_int_struct() {
        let value = test_conformance::GoldenIntStruct { v: 1i16 };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_uint_struct() {
        let value = test_conformance::GoldenUintStruct { v: 1u16 };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_float_struct() {
        let value = test_conformance::GoldenFloatStruct { v: 0f32 };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_double_struct() {
        let value = test_conformance::GoldenDoubleStruct { v: 0f64 };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_string_struct() {
        let value = test_conformance::GoldenStringStruct { v: String::from("abcd") };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x04,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x61,0x62,0x63,0x64,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_nullable_string_struct_non_null() {
        let value = test_conformance::GoldenNullableStringStruct { v: Some(String::from("abcd")) };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x04,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x61,0x62,0x63,0x64,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_nullable_string_struct_null() {
        let value = test_conformance::GoldenNullableStringStruct { v: None };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_enum_struct() {
        let value = test_conformance::GoldenEnumStruct { v: test_conformance::GoldenEnum::try_from(1u16).unwrap() };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_bits_struct() {
        let value = test_conformance::GoldenBitsStruct { v: test_conformance::GoldenBits::from_bits_retain(1u16) };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_table_struct() {
        let value = test_conformance::GoldenTableStruct { v: test_conformance::GoldenTable { v: Some(1i16), ..Default::default() } };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x01,0x00,0x00,0x00,0x00,0x00,0x01,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_union_struct() {
        let value = test_conformance::GoldenUnionStruct { v: test_conformance::GoldenUnion::V(1i16) };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0x01,0x00,0x00,0x00,0x00,0x00,0x01,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_nullable_union_struct_non_null() {
        let value = test_conformance::GoldenNullableUnionStruct { v: Some(Box::new(test_conformance::GoldenUnion::V(1i16))) };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0x01,0x00,0x00,0x00,0x00,0x00,0x01,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_nullable_union_struct_null() {
        let value = test_conformance::GoldenNullableUnionStruct { v: None };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_byte_array_struct() {
        let value = test_conformance::GoldenByteArrayStruct { v: [1u8, 2u8, 3u8, 4u8] };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x01,0x02,0x03,0x04,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_struct_array_struct() {
        let value = test_conformance::GoldenStructArrayStruct { v: [test_conformance::GoldenIntStruct { v: 1i16 }, test_conformance::GoldenIntStruct { v: 2i16 }] };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x01,0x00,0x02,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_byte_vector_struct() {
        let value = test_conformance::GoldenByteVectorStruct { v: vec![1u8, 2u8, 3u8, 4u8, 1u8, 2u8, 3u8, 4u8, 1u8, 2u8, 3u8, 4u8] };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x0c,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x01,0x02,0x03,0x04,0x01,0x02,0x03,0x04,
0x01,0x02,0x03,0x04,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_byte_nullable_vector_struct_non_null() {
        let value = test_conformance::GoldenNullableByteVectorStruct { v: Some(vec![1u8, 2u8, 3u8, 4u8]) };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x04,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x01,0x02,0x03,0x04,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_nullable_byte_vector_struct_null() {
        let value = test_conformance::GoldenNullableByteVectorStruct { v: None };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_struct_vector_struct() {
        let value = test_conformance::GoldenStructVectorStruct { v: vec![test_conformance::GoldenIntStruct { v: 1i16 }, test_conformance::GoldenIntStruct { v: 2i16 }] };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x02,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x01,0x00,0x02,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_nullable_struct_non_null() {
        let value = test_conformance::GoldenNullableStruct { v: Some(Box::new(test_conformance::GoldenBoolStruct { v: true })) };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_nullable_struct_null() {
        let value = test_conformance::GoldenNullableStruct { v: None };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }

    #[test]
    fn encode_golden_nullable_handle_struct_non_null() {
            let handle_defs = create_handles(&[
// #0
HandleDef{
	subtype: HandleSubtype::Event,
	rights: fidl_next::fuchsia::zx::Rights::from_bits(2147483648).unwrap(),
},
]);
        let value = test_conformance::GoldenNullableHandleStruct { v: Some(copy_handle(&handle_defs[0])) };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0xff,0xff,0xff,0xff,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(
            buffer.handles().iter().map(|h| h.raw_handle()).collect::<Vec<_>>(),
            &[handle_defs[0].handle,
]
        );
    }

    #[test]
    fn encode_golden_nullable_handle_struct_null() {
        let value = test_conformance::GoldenNullableHandleStruct { v: None };
        let mut buffer = Buffer::new();
        buffer.encode_next(value, ()).unwrap();
        assert_eq!(&[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
], buffer.bytes().as_slice());
        // TODO: support handle dispositions
        assert_eq!(buffer.handles(), &[]);
    }



    #[test]
    fn decode_golden_bool_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenBoolStruct = recv_buffer.decode::<test_conformance::wire::GoldenBoolStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenBoolStruct { v: true });

    }

    #[test]
    fn decode_golden_int_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenIntStruct = recv_buffer.decode::<test_conformance::wire::GoldenIntStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenIntStruct { v: 1i16 });

    }

    #[test]
    fn decode_golden_uint_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenUintStruct = recv_buffer.decode::<test_conformance::wire::GoldenUintStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenUintStruct { v: 1u16 });

    }

    #[test]
    fn decode_golden_float_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenFloatStruct = recv_buffer.decode::<test_conformance::wire::GoldenFloatStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenFloatStruct { v: 0f32 });

    }

    #[test]
    fn decode_golden_double_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenDoubleStruct = recv_buffer.decode::<test_conformance::wire::GoldenDoubleStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenDoubleStruct { v: 0f64 });

    }

    #[test]
    fn decode_golden_string_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x04,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x61,0x62,0x63,0x64,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenStringStruct = recv_buffer.decode::<test_conformance::wire::GoldenStringStruct<'static>>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenStringStruct { v: String::from("abcd") });

    }

    #[test]
    fn decode_golden_nullable_string_struct_non_null() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x04,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x61,0x62,0x63,0x64,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenNullableStringStruct = recv_buffer.decode::<test_conformance::wire::GoldenNullableStringStruct<'static>>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenNullableStringStruct { v: Some(String::from("abcd")) });

    }

    #[test]
    fn decode_golden_nullable_string_struct_null() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenNullableStringStruct = recv_buffer.decode::<test_conformance::wire::GoldenNullableStringStruct<'static>>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenNullableStringStruct { v: None });

    }

    #[test]
    fn decode_golden_enum_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenEnumStruct = recv_buffer.decode::<test_conformance::wire::GoldenEnumStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenEnumStruct { v: test_conformance::GoldenEnum::try_from(1u16).unwrap() });

    }

    #[test]
    fn decode_golden_bits_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenBitsStruct = recv_buffer.decode::<test_conformance::wire::GoldenBitsStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenBitsStruct { v: test_conformance::GoldenBits::from_bits_retain(1u16) });

    }

    #[test]
    fn decode_golden_table_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x01,0x00,0x00,0x00,0x00,0x00,0x01,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenTableStruct = recv_buffer.decode::<test_conformance::wire::GoldenTableStruct<'static>>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenTableStruct { v: test_conformance::GoldenTable { v: Some(1i16), ..Default::default() } });

    }

    #[test]
    fn decode_golden_union_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0x01,0x00,0x00,0x00,0x00,0x00,0x01,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenUnionStruct = recv_buffer.decode::<test_conformance::wire::GoldenUnionStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenUnionStruct { v: test_conformance::GoldenUnion::V(1i16) });

    }

    #[test]
    fn decode_golden_nullable_union_struct_non_null() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0x01,0x00,0x00,0x00,0x00,0x00,0x01,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenNullableUnionStruct = recv_buffer.decode::<test_conformance::wire::GoldenNullableUnionStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenNullableUnionStruct { v: Some(Box::new(test_conformance::GoldenUnion::V(1i16))) });

    }

    #[test]
    fn decode_golden_nullable_union_struct_null() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenNullableUnionStruct = recv_buffer.decode::<test_conformance::wire::GoldenNullableUnionStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenNullableUnionStruct { v: None });

    }

    #[test]
    fn decode_golden_byte_array_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x01,0x02,0x03,0x04,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenByteArrayStruct = recv_buffer.decode::<test_conformance::wire::GoldenByteArrayStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenByteArrayStruct { v: [1u8, 2u8, 3u8, 4u8] });

    }

    #[test]
    fn decode_golden_struct_array_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x01,0x00,0x02,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenStructArrayStruct = recv_buffer.decode::<test_conformance::wire::GoldenStructArrayStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenStructArrayStruct { v: [test_conformance::GoldenIntStruct { v: 1i16 }, test_conformance::GoldenIntStruct { v: 2i16 }] });

    }

    #[test]
    fn decode_golden_byte_vector_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x0c,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x01,0x02,0x03,0x04,0x01,0x02,0x03,0x04,
0x01,0x02,0x03,0x04,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenByteVectorStruct = recv_buffer.decode::<test_conformance::wire::GoldenByteVectorStruct<'static>>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenByteVectorStruct { v: vec![1u8, 2u8, 3u8, 4u8, 1u8, 2u8, 3u8, 4u8, 1u8, 2u8, 3u8, 4u8] });

    }

    #[test]
    fn decode_golden_byte_nullable_vector_struct_non_null() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x04,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x01,0x02,0x03,0x04,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenNullableByteVectorStruct = recv_buffer.decode::<test_conformance::wire::GoldenNullableByteVectorStruct<'static>>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenNullableByteVectorStruct { v: Some(vec![1u8, 2u8, 3u8, 4u8]) });

    }

    #[test]
    fn decode_golden_nullable_byte_vector_struct_null() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenNullableByteVectorStruct = recv_buffer.decode::<test_conformance::wire::GoldenNullableByteVectorStruct<'static>>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenNullableByteVectorStruct { v: None });

    }

    #[test]
    fn decode_golden_struct_vector_struct() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x02,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x01,0x00,0x02,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenStructVectorStruct = recv_buffer.decode::<test_conformance::wire::GoldenStructVectorStruct<'static>>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenStructVectorStruct { v: vec![test_conformance::GoldenIntStruct { v: 1i16 }, test_conformance::GoldenIntStruct { v: 2i16 }] });

    }

    #[test]
    fn decode_golden_nullable_struct_non_null() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x01,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenNullableStruct = recv_buffer.decode::<test_conformance::wire::GoldenNullableStruct<'static>>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenNullableStruct { v: Some(Box::new(test_conformance::GoldenBoolStruct { v: true })) });

    }

    #[test]
    fn decode_golden_nullable_struct_null() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenNullableStruct = recv_buffer.decode::<test_conformance::wire::GoldenNullableStruct<'static>>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenNullableStruct { v: None });

    }

    #[test]
    fn decode_golden_handle_basic_rights_struct() {
            let handle_defs = create_handles(&[
// #0
HandleDef{
	subtype: HandleSubtype::Event,
	rights: fidl_next::fuchsia::zx::Rights::from_bits(53251).unwrap(),
},
]);
            let _handle_koids = handle_defs.iter().map(get_handle_koid).collect::<Vec<_>>();
            #[allow(unused)]
            let mut handle_infos = select_handle_infos(&handle_defs, &[
0,]);

        let buffer = Buffer::from_raw_bytes(vec![
copy_handle(&handle_defs[0]),], &[
0xff,0xff,0xff,0xff,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenHandleBasicRightsStruct = recv_buffer.decode::<test_conformance::wire::GoldenHandleBasicRightsStruct>().unwrap().take();
        
    }

    #[test]
    fn decode_golden_nullable_handle_struct_non_null() {
            let handle_defs = create_handles(&[
// #0
HandleDef{
	subtype: HandleSubtype::Event,
	rights: fidl_next::fuchsia::zx::Rights::from_bits(2147483648).unwrap(),
},
]);
            let _handle_koids = handle_defs.iter().map(get_handle_koid).collect::<Vec<_>>();
            #[allow(unused)]
            let mut handle_infos = select_handle_infos(&handle_defs, &[
0,]);

        let buffer = Buffer::from_raw_bytes(vec![
copy_handle(&handle_defs[0]),], &[
0xff,0xff,0xff,0xff,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenNullableHandleStruct = recv_buffer.decode::<test_conformance::wire::GoldenNullableHandleStruct>().unwrap().take();
        assert_eq!(_value.v.as_ref().unwrap().basic_info().unwrap().koid.raw_koid(), _handle_koids[0]);

    }

    #[test]
    fn decode_golden_nullable_handle_struct_null() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenNullableHandleStruct = recv_buffer.decode::<test_conformance::wire::GoldenNullableHandleStruct>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenNullableHandleStruct { v: None });

    }

    #[test]
    fn decode_golden_table_unknown_dropped() {
            let handle_defs = create_handles(&[
// #0
HandleDef{
	subtype: HandleSubtype::Event,
	rights: fidl_next::fuchsia::zx::Rights::from_bits(2147483648).unwrap(),
},
]);
            let _handle_koids = handle_defs.iter().map(get_handle_koid).collect::<Vec<_>>();
            #[allow(unused)]
            let mut handle_infos = select_handle_infos(&handle_defs, &[
0,]);

        let buffer = Buffer::from_raw_bytes(vec![
copy_handle(&handle_defs[0]),], &[
0x02,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0xff,0xff,0xff,0xff,
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0xff,0xff,0xff,0xff,0x01,0x00,0x01,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);
        let _value: test_conformance::GoldenTableStruct = recv_buffer.decode::<test_conformance::wire::GoldenTableStruct<'static>>().unwrap().take();
        assert_eq!(_value, test_conformance::GoldenTableStruct { v: test_conformance::GoldenTable { ..Default::default() } });

        let unused_handles = select_raw_handle_infos(&handle_defs, &[
0,]);
        assert_eq!(
            unused_handles.iter().map(get_info_handle_valid).collect::<Vec<_>>(),
            repeat(Err(Status::BAD_HANDLE), unused_handles.len()),
        );
    }



    #[test]
    fn encode_failure_golden_string_with_max_size2() {

        let mut buffer = Buffer::new();
        let value = test_conformance::GoldenStringWithMaxSize2 { s: String::from("abc") };
        match buffer.encode_next(value, ()) {
            Err(err) => assert_matches!(err, EncodeError::Validation(ValidationError::StringTooLong{ .. })),
            Ok(_) => panic!("unexpected successful encoding"),
        }
        // TODO: check that we get the right error.
        // TODO: check that handles are closed
    }



    #[test]
    fn decode_failure_golden_string_struct_null_body() {

        let buffer = Buffer::from_raw_bytes(vec![
], &[
0x03,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
]);
        let recv_buffer = RecvBuffer::new(buffer);

        match recv_buffer.decode::<test_conformance::wire::GoldenStringStruct<'static>>() {
            Err(err) => assert_matches!(err, DecodeError::InvalidOptionalSize(_) | DecodeError::RequiredValueAbsent),
            Ok(value) => panic!("unexpected successful decoding to: {:?}", value.take()),
        }

    }


