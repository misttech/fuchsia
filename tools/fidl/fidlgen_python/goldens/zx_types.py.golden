# WARNING: This file is machine generated by fidlgen.

from __future__ import annotations

from dataclasses import dataclass
import sys
import typing
import enum

import fidl


from fidl._client import EventHandlerBase, FidlClient  # type: ignore[unused-ignore,unused-import]
from fidl._server import ServerBase  # type: ignore[unused-ignore,unused-import]
from fidl._fidl_common import DomainError, FrameworkError, MethodInfo, FidlProtocolMarker, normalize_identifier  # type: ignore[unused-ignore,unused-import]

from abc import abstractmethod, ABC  # type: ignore[unused-ignore,unused-import]
import collections.abc  # type: ignore[unused-ignore,unused-import]


_UNDEFINED: typing.Any = object()


@dataclass
class StatusProtocolARequest:
    arg: int

    __fidl_kind__ = "struct"
    __fidl_type__ = "StatusProtocolARequest"
    __fidl_raw_type__ = "test.zxtypes/StatusProtocolARequest"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 4)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 4)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        encoder.write_int32(self.arg, _offset + 0)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        arg = decoder.read_int32(_offset + 0)
        return cls(
            arg=arg,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "arg": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class StatusProtocolBRequest:
    arg: int

    __fidl_kind__ = "struct"
    __fidl_type__ = "StatusProtocolBRequest"
    __fidl_raw_type__ = "test.zxtypes/StatusProtocolBRequest"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 4)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 4)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        encoder.write_int32(self.arg, _offset + 0)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        arg = decoder.read_int32(_offset + 0)
        return cls(
            arg=arg,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "arg": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class StatusProtocolAResponse:
    result: int

    __fidl_kind__ = "struct"
    __fidl_type__ = "StatusProtocolAResponse"
    __fidl_raw_type__ = "test.zxtypes/StatusProtocol_A_Response"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 4)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 4)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        encoder.write_int32(self.result, _offset + 0)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        result = decoder.read_int32(_offset + 0)
        return cls(
            result=result,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "result": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class StatusProtocolBResponse:
    result: int

    __fidl_kind__ = "struct"
    __fidl_type__ = "StatusProtocolBResponse"
    __fidl_raw_type__ = "test.zxtypes/StatusProtocol_B_Response"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 4)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 4)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        encoder.write_int32(self.result, _offset + 0)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        result = decoder.read_int32(_offset + 0)
        return cls(
            result=result,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "result": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class StatusStruct:
    status: int
    status_array: typing.Sequence[int]
    status_vector: typing.Sequence[int]

    __fidl_kind__ = "struct"
    __fidl_type__ = "StatusStruct"
    __fidl_raw_type__ = "test.zxtypes/StatusStruct"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 64)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 64)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        encoder.write_int32(self.status, _offset + 0)
        encoder.write_array(
            self.status_array,
            _offset + 4,
            lambda val, enc, off: enc.write_int32(val, off),
            4,
            10,
        )
        encoder.write_vector(
            self.status_vector,
            _offset + 48,
            lambda val, enc, off: enc.write_int32(val, off),
            4,
        )

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        status = decoder.read_int32(_offset + 0)
        status_array = decoder.read_array(
            _offset + 4, lambda dec, off: dec.read_int32(off), 4, 10
        )
        status_vector = decoder.read_vector(
            _offset + 48,
            lambda dec, off: dec.read_int32(off),
            4,
            nullable=False,
        )
        return cls(
            status=status,
            status_array=status_array,
            status_vector=status_vector,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "status": None,  # type: ignore[arg-type,unused-ignore]
                "status_array": None,  # type: ignore[arg-type,unused-ignore]
                "status_vector": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class StatusStructAlias:
    status: int
    status_array: typing.Sequence[int]
    status_vector: typing.Sequence[int]

    __fidl_kind__ = "struct"
    __fidl_type__ = "StatusStructAlias"
    __fidl_raw_type__ = "test.zxtypes/StatusStructAlias"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 64)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 64)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        encoder.write_int32(self.status, _offset + 0)
        encoder.write_array(
            self.status_array,
            _offset + 4,
            lambda val, enc, off: enc.write_int32(val, off),
            4,
            10,
        )
        encoder.write_vector(
            self.status_vector,
            _offset + 48,
            lambda val, enc, off: enc.write_int32(val, off),
            4,
        )

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        status = decoder.read_int32(_offset + 0)
        status_array = decoder.read_array(
            _offset + 4, lambda dec, off: dec.read_int32(off), 4, 10
        )
        status_vector = decoder.read_vector(
            _offset + 48,
            lambda dec, off: dec.read_int32(off),
            4,
            nullable=False,
        )
        return cls(
            status=status,
            status_array=status_array,
            status_vector=status_vector,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "status": None,  # type: ignore[arg-type,unused-ignore]
                "status_array": None,  # type: ignore[arg-type,unused-ignore]
                "status_vector": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class StatusTable:
    status: int | None
    status_array: typing.Sequence[int] | None
    status_vector: typing.Sequence[int] | None

    def __init__(
        self,
        status: int | None = None,
        status_array: typing.Sequence[int] | None = None,
        status_vector: typing.Sequence[int] | None = None,
    ) -> None:
        self.status = status
        self.status_array = status_array
        self.status_vector = status_vector

    __fidl_kind__ = "table"
    __fidl_type__ = "StatusTable"
    __fidl_raw_type__ = "test.zxtypes/StatusTable"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, offset: int) -> None:
        max_ord = 0
        if self.status is not None:
            max_ord = max(max_ord, 1)
        if self.status_array is not None:
            max_ord = max(max_ord, 2)
        if self.status_vector is not None:
            max_ord = max(max_ord, 3)

        if max_ord == 0:
            encoder.write_inline(offset, b"\x00" * 8 + b"\xff" * 8)
            return

        encoder.write_uint64(max_ord, offset)
        encoder.write_uint64(0xFFFFFFFFFFFFFFFF, offset + 8)

        def encode_envelopes(
            val: typing.Any, enc: fidl.Encoder, start_offset: int
        ) -> None:
            if max_ord >= 1:
                enc.write_envelope(
                    val.status,
                    start_offset + 0,
                    lambda val, enc, off: enc.write_int32(val, off),
                    4,
                    True,
                )
            if max_ord >= 2:
                enc.write_envelope(
                    val.status_array,
                    start_offset + 8,
                    lambda val, enc, off: enc.write_array(
                        val,
                        off,
                        lambda val, enc, off: enc.write_int32(val, off),
                        4,
                        10,
                    ),
                    40,
                    False,
                )
            if max_ord >= 3:
                enc.write_envelope(
                    val.status_vector,
                    start_offset + 16,
                    lambda val, enc, off: enc.write_vector(
                        val,
                        off,
                        lambda val, enc, off: enc.write_int32(val, off),
                        4,
                    ),
                    16,
                    False,
                )

        encoder.ool_tasks.append(
            lambda: encoder.write_ool_struct(
                self, encode_envelopes, max_ord * 8
            )
        )

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        _max_ord = decoder.read_uint64(_offset)
        _presence = decoder.read_uint64(_offset + 8)
        if _presence == 0:
            return cls()

        _pad_mod = decoder.ool_offset % 8
        if _pad_mod != 0:
            decoder.ool_offset += 8 - _pad_mod
        _env_array_start = decoder.ool_offset
        decoder.ool_offset += _max_ord * 8

        status = None
        status_array = None
        status_vector = None
        for _ord_idx in range(1, _max_ord + 1):
            _env_offset = _env_array_start + (_ord_idx - 1) * 8
            if _ord_idx == 1:
                status = decoder.read_envelope(
                    _env_offset, lambda dec, off: dec.read_int32(off), 4
                )
            elif _ord_idx == 2:
                status_array = decoder.read_envelope(
                    _env_offset,
                    lambda dec, off: dec.read_array(
                        off, lambda dec, off: dec.read_int32(off), 4, 10
                    ),
                    40,
                )
            elif _ord_idx == 3:
                status_vector = decoder.read_envelope(
                    _env_offset,
                    lambda dec, off: dec.read_vector(
                        off,
                        lambda dec, off: dec.read_int32(off),
                        4,
                        nullable=False,
                    ),
                    16,
                )
            else:
                decoder.skip_envelope(_env_offset)

        return cls(
            status=status,
            status_array=status_array,
            status_vector=status_vector,
        )

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls()


@dataclass
class StatusTableAlias:
    status: int | None
    status_array: typing.Sequence[int] | None
    status_vector: typing.Sequence[int] | None

    def __init__(
        self,
        status: int | None = None,
        status_array: typing.Sequence[int] | None = None,
        status_vector: typing.Sequence[int] | None = None,
    ) -> None:
        self.status = status
        self.status_array = status_array
        self.status_vector = status_vector

    __fidl_kind__ = "table"
    __fidl_type__ = "StatusTableAlias"
    __fidl_raw_type__ = "test.zxtypes/StatusTableAlias"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, offset: int) -> None:
        max_ord = 0
        if self.status is not None:
            max_ord = max(max_ord, 1)
        if self.status_array is not None:
            max_ord = max(max_ord, 2)
        if self.status_vector is not None:
            max_ord = max(max_ord, 3)

        if max_ord == 0:
            encoder.write_inline(offset, b"\x00" * 8 + b"\xff" * 8)
            return

        encoder.write_uint64(max_ord, offset)
        encoder.write_uint64(0xFFFFFFFFFFFFFFFF, offset + 8)

        def encode_envelopes(
            val: typing.Any, enc: fidl.Encoder, start_offset: int
        ) -> None:
            if max_ord >= 1:
                enc.write_envelope(
                    val.status,
                    start_offset + 0,
                    lambda val, enc, off: enc.write_int32(val, off),
                    4,
                    True,
                )
            if max_ord >= 2:
                enc.write_envelope(
                    val.status_array,
                    start_offset + 8,
                    lambda val, enc, off: enc.write_array(
                        val,
                        off,
                        lambda val, enc, off: enc.write_int32(val, off),
                        4,
                        10,
                    ),
                    40,
                    False,
                )
            if max_ord >= 3:
                enc.write_envelope(
                    val.status_vector,
                    start_offset + 16,
                    lambda val, enc, off: enc.write_vector(
                        val,
                        off,
                        lambda val, enc, off: enc.write_int32(val, off),
                        4,
                    ),
                    16,
                    False,
                )

        encoder.ool_tasks.append(
            lambda: encoder.write_ool_struct(
                self, encode_envelopes, max_ord * 8
            )
        )

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        _max_ord = decoder.read_uint64(_offset)
        _presence = decoder.read_uint64(_offset + 8)
        if _presence == 0:
            return cls()

        _pad_mod = decoder.ool_offset % 8
        if _pad_mod != 0:
            decoder.ool_offset += 8 - _pad_mod
        _env_array_start = decoder.ool_offset
        decoder.ool_offset += _max_ord * 8

        status = None
        status_array = None
        status_vector = None
        for _ord_idx in range(1, _max_ord + 1):
            _env_offset = _env_array_start + (_ord_idx - 1) * 8
            if _ord_idx == 1:
                status = decoder.read_envelope(
                    _env_offset, lambda dec, off: dec.read_int32(off), 4
                )
            elif _ord_idx == 2:
                status_array = decoder.read_envelope(
                    _env_offset,
                    lambda dec, off: dec.read_array(
                        off, lambda dec, off: dec.read_int32(off), 4, 10
                    ),
                    40,
                )
            elif _ord_idx == 3:
                status_vector = decoder.read_envelope(
                    _env_offset,
                    lambda dec, off: dec.read_vector(
                        off,
                        lambda dec, off: dec.read_int32(off),
                        4,
                        nullable=False,
                    ),
                    16,
                )
            else:
                decoder.skip_envelope(_env_offset)

        return cls(
            status=status,
            status_array=status_array,
            status_vector=status_vector,
        )

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls()


class StatusProtocolAResult:
    _response: typing.Optional[StatusProtocolAResponse]
    _err: typing.Optional[int]
    _framework_err: typing.Optional[fidl.FrameworkError]

    __fidl_kind__ = "union"
    __fidl_type__ = "StatusProtocolAResult"
    __fidl_raw_type__ = "test.zxtypes/StatusProtocol_A_Result"
    _is_result = True

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.response != other.response:
            return False
        if self.err != other.err:
            return False
        if self.framework_err != other.framework_err:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.response:
            variant = f"response={self.response!r}"
        if self.err:
            variant = f"err={self.err!r}"
        if self.framework_err:
            variant = f"framework_err={self.framework_err!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        response: typing.Optional[StatusProtocolAResponse] = _UNDEFINED,
        err: typing.Optional[int] = _UNDEFINED,
        framework_err: typing.Optional[fidl.FrameworkError] = _UNDEFINED,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if response is not _UNDEFINED:
            if response is not None:
                self._response = response
                variants.append("response")
                number_of_variants += 1
        if err is not _UNDEFINED:
            if err is not None:
                self._err = err
                variants.append("err")
                number_of_variants += 1
        if framework_err is not _UNDEFINED:
            if framework_err is not None:
                self._framework_err = framework_err
                variants.append("framework_err")
                number_of_variants += 1
        if number_of_variants > 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def response(self) -> StatusProtocolAResponse | None:
        return getattr(self, "_response", None)

    @property
    def err(self) -> int | None:
        return getattr(self, "_err", None)

    @property
    def framework_err(self) -> fidl.FrameworkError | None:
        return getattr(self, "_framework_err", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        if hasattr(self, "_response"):
            encoder.write_uint64(1, _offset)
            encoder.write_envelope(
                getattr(self, "_response"),
                _offset + 8,
                StatusProtocolAResponse._encode,
                4,
                True,
            )
            return
        if hasattr(self, "_err"):
            encoder.write_uint64(2, _offset)
            encoder.write_envelope(
                getattr(self, "_err"),
                _offset + 8,
                lambda val, enc, off: enc.write_int32(val, off),
                4,
                True,
            )
            return
        if hasattr(self, "_framework_err"):
            encoder.write_uint64(3, _offset)
            encoder.write_envelope(
                getattr(self, "_framework_err"),
                _offset + 8,
                lambda val, enc, off: enc.write_int32(val, off),
                4,
                True,
            )
            return
        encoder.write_inline(_offset, b"\x00" * 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        ordinal = decoder.read_uint64(_offset)
        if ordinal == 0:
            return cls(_empty=())
        if ordinal == 1:
            val = decoder.read_envelope(
                _offset + 8,
                StatusProtocolAResponse._decode,
                4,
            )
            return cls(response=val)
        if ordinal == 2:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_int32(off),
                4,
            )
            return cls(err=val)
        if ordinal == 3:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: FrameworkError(dec.read_int32(off)),
                4,
            )
            return cls(framework_err=val)

        # Skip unknown envelope
        decoder.skip_envelope(_offset + 8)
        raise ValueError(f"Unknown union ordinal {ordinal}")

    def unwrap(self) -> StatusProtocolAResponse:
        """Returns the response if result does not contain an error. Otherwise, raises an exception."""
        try:
            if (
                hasattr(self, "_framework_err")
                and self._framework_err is not None
            ):
                raise AssertionError(
                    f"{self.__fidl_raw_type__} framework error {self._framework_err}"
                )
        except AttributeError:
            pass
        try:
            if hasattr(self, "_err") and self._err is not None:
                raise AssertionError(
                    f"{self.__fidl_raw_type__} error {self._err}"
                )
        except AttributeError:
            pass

        assert (
            self._response is not None
        ), f"Failed to unwrap {self.__fidl_raw_type__}. Result does not contain an error or response."
        return self._response

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


class StatusProtocolBResult:
    _response: typing.Optional[StatusProtocolBResponse]
    _err: typing.Optional[int]
    _framework_err: typing.Optional[fidl.FrameworkError]

    __fidl_kind__ = "union"
    __fidl_type__ = "StatusProtocolBResult"
    __fidl_raw_type__ = "test.zxtypes/StatusProtocol_B_Result"
    _is_result = True

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.response != other.response:
            return False
        if self.err != other.err:
            return False
        if self.framework_err != other.framework_err:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.response:
            variant = f"response={self.response!r}"
        if self.err:
            variant = f"err={self.err!r}"
        if self.framework_err:
            variant = f"framework_err={self.framework_err!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        response: typing.Optional[StatusProtocolBResponse] = _UNDEFINED,
        err: typing.Optional[int] = _UNDEFINED,
        framework_err: typing.Optional[fidl.FrameworkError] = _UNDEFINED,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if response is not _UNDEFINED:
            if response is not None:
                self._response = response
                variants.append("response")
                number_of_variants += 1
        if err is not _UNDEFINED:
            if err is not None:
                self._err = err
                variants.append("err")
                number_of_variants += 1
        if framework_err is not _UNDEFINED:
            if framework_err is not None:
                self._framework_err = framework_err
                variants.append("framework_err")
                number_of_variants += 1
        if number_of_variants > 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def response(self) -> StatusProtocolBResponse | None:
        return getattr(self, "_response", None)

    @property
    def err(self) -> int | None:
        return getattr(self, "_err", None)

    @property
    def framework_err(self) -> fidl.FrameworkError | None:
        return getattr(self, "_framework_err", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        if hasattr(self, "_response"):
            encoder.write_uint64(1, _offset)
            encoder.write_envelope(
                getattr(self, "_response"),
                _offset + 8,
                StatusProtocolBResponse._encode,
                4,
                True,
            )
            return
        if hasattr(self, "_err"):
            encoder.write_uint64(2, _offset)
            encoder.write_envelope(
                getattr(self, "_err"),
                _offset + 8,
                lambda val, enc, off: enc.write_int32(val, off),
                4,
                True,
            )
            return
        if hasattr(self, "_framework_err"):
            encoder.write_uint64(3, _offset)
            encoder.write_envelope(
                getattr(self, "_framework_err"),
                _offset + 8,
                lambda val, enc, off: enc.write_int32(val, off),
                4,
                True,
            )
            return
        encoder.write_inline(_offset, b"\x00" * 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        ordinal = decoder.read_uint64(_offset)
        if ordinal == 0:
            return cls(_empty=())
        if ordinal == 1:
            val = decoder.read_envelope(
                _offset + 8,
                StatusProtocolBResponse._decode,
                4,
            )
            return cls(response=val)
        if ordinal == 2:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_int32(off),
                4,
            )
            return cls(err=val)
        if ordinal == 3:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: FrameworkError(dec.read_int32(off)),
                4,
            )
            return cls(framework_err=val)

        # Skip unknown envelope
        decoder.skip_envelope(_offset + 8)
        raise ValueError(f"Unknown union ordinal {ordinal}")

    def unwrap(self) -> StatusProtocolBResponse:
        """Returns the response if result does not contain an error. Otherwise, raises an exception."""
        try:
            if (
                hasattr(self, "_framework_err")
                and self._framework_err is not None
            ):
                raise AssertionError(
                    f"{self.__fidl_raw_type__} framework error {self._framework_err}"
                )
        except AttributeError:
            pass
        try:
            if hasattr(self, "_err") and self._err is not None:
                raise AssertionError(
                    f"{self.__fidl_raw_type__} error {self._err}"
                )
        except AttributeError:
            pass

        assert (
            self._response is not None
        ), f"Failed to unwrap {self.__fidl_raw_type__}. Result does not contain an error or response."
        return self._response

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


class StatusUnion:
    _status: typing.Optional[int]
    _status_array: typing.Optional[typing.Sequence[int]]
    _status_vector: typing.Optional[typing.Sequence[int]]

    __fidl_kind__ = "union"
    __fidl_type__ = "StatusUnion"
    __fidl_raw_type__ = "test.zxtypes/StatusUnion"
    _is_result = False

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.status != other.status:
            return False
        if self.status_array != other.status_array:
            return False
        if self.status_vector != other.status_vector:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.status:
            variant = f"status={self.status!r}"
        if self.status_array:
            variant = f"status_array={self.status_array!r}"
        if self.status_vector:
            variant = f"status_vector={self.status_vector!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        status: typing.Optional[int] = _UNDEFINED,
        status_array: typing.Optional[typing.Sequence[int]] = _UNDEFINED,
        status_vector: typing.Optional[typing.Sequence[int]] = _UNDEFINED,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if status is not _UNDEFINED:
            if status is not None:
                self._status = status
                variants.append("status")
                number_of_variants += 1
        if status_array is not _UNDEFINED:
            if status_array is not None:
                self._status_array = status_array
                variants.append("status_array")
                number_of_variants += 1
        if status_vector is not _UNDEFINED:
            if status_vector is not None:
                self._status_vector = status_vector
                variants.append("status_vector")
                number_of_variants += 1
        if number_of_variants != 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def status(self) -> int | None:
        return getattr(self, "_status", None)

    @property
    def status_array(self) -> typing.Sequence[int] | None:
        return getattr(self, "_status_array", None)

    @property
    def status_vector(self) -> typing.Sequence[int] | None:
        return getattr(self, "_status_vector", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        if hasattr(self, "_status"):
            encoder.write_uint64(1, _offset)
            encoder.write_envelope(
                getattr(self, "_status"),
                _offset + 8,
                lambda val, enc, off: enc.write_int32(val, off),
                4,
                True,
            )
            return
        if hasattr(self, "_status_array"):
            encoder.write_uint64(2, _offset)
            encoder.write_envelope(
                getattr(self, "_status_array"),
                _offset + 8,
                lambda val, enc, off: enc.write_array(
                    val,
                    off,
                    lambda val, enc, off: enc.write_int32(val, off),
                    4,
                    10,
                ),
                40,
                False,
            )
            return
        if hasattr(self, "_status_vector"):
            encoder.write_uint64(3, _offset)
            encoder.write_envelope(
                getattr(self, "_status_vector"),
                _offset + 8,
                lambda val, enc, off: enc.write_vector(
                    val, off, lambda val, enc, off: enc.write_int32(val, off), 4
                ),
                16,
                False,
            )
            return
        encoder.write_inline(_offset, b"\x00" * 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        ordinal = decoder.read_uint64(_offset)
        if ordinal == 0:
            return cls(_empty=())
        if ordinal == 1:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_int32(off),
                4,
            )
            return cls(status=val)
        if ordinal == 2:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_array(
                    off, lambda dec, off: dec.read_int32(off), 4, 10
                ),
                40,
            )
            return cls(status_array=val)
        if ordinal == 3:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_vector(
                    off, lambda dec, off: dec.read_int32(off), 4, nullable=False
                ),
                16,
            )
            return cls(status_vector=val)

        # Skip unknown envelope
        decoder.skip_envelope(_offset + 8)
        raise ValueError(f"Unknown union ordinal {ordinal}")

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


class StatusUnionAlias:
    _status: typing.Optional[int]
    _status_array: typing.Optional[typing.Sequence[int]]
    _status_vector: typing.Optional[typing.Sequence[int]]

    __fidl_kind__ = "union"
    __fidl_type__ = "StatusUnionAlias"
    __fidl_raw_type__ = "test.zxtypes/StatusUnionAlias"
    _is_result = False

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.status != other.status:
            return False
        if self.status_array != other.status_array:
            return False
        if self.status_vector != other.status_vector:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.status:
            variant = f"status={self.status!r}"
        if self.status_array:
            variant = f"status_array={self.status_array!r}"
        if self.status_vector:
            variant = f"status_vector={self.status_vector!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        status: typing.Optional[int] = _UNDEFINED,
        status_array: typing.Optional[typing.Sequence[int]] = _UNDEFINED,
        status_vector: typing.Optional[typing.Sequence[int]] = _UNDEFINED,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if status is not _UNDEFINED:
            if status is not None:
                self._status = status
                variants.append("status")
                number_of_variants += 1
        if status_array is not _UNDEFINED:
            if status_array is not None:
                self._status_array = status_array
                variants.append("status_array")
                number_of_variants += 1
        if status_vector is not _UNDEFINED:
            if status_vector is not None:
                self._status_vector = status_vector
                variants.append("status_vector")
                number_of_variants += 1
        if number_of_variants != 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def status(self) -> int | None:
        return getattr(self, "_status", None)

    @property
    def status_array(self) -> typing.Sequence[int] | None:
        return getattr(self, "_status_array", None)

    @property
    def status_vector(self) -> typing.Sequence[int] | None:
        return getattr(self, "_status_vector", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        if hasattr(self, "_status"):
            encoder.write_uint64(1, _offset)
            encoder.write_envelope(
                getattr(self, "_status"),
                _offset + 8,
                lambda val, enc, off: enc.write_int32(val, off),
                4,
                True,
            )
            return
        if hasattr(self, "_status_array"):
            encoder.write_uint64(2, _offset)
            encoder.write_envelope(
                getattr(self, "_status_array"),
                _offset + 8,
                lambda val, enc, off: enc.write_array(
                    val,
                    off,
                    lambda val, enc, off: enc.write_int32(val, off),
                    4,
                    10,
                ),
                40,
                False,
            )
            return
        if hasattr(self, "_status_vector"):
            encoder.write_uint64(3, _offset)
            encoder.write_envelope(
                getattr(self, "_status_vector"),
                _offset + 8,
                lambda val, enc, off: enc.write_vector(
                    val, off, lambda val, enc, off: enc.write_int32(val, off), 4
                ),
                16,
                False,
            )
            return
        encoder.write_inline(_offset, b"\x00" * 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        ordinal = decoder.read_uint64(_offset)
        if ordinal == 0:
            return cls(_empty=())
        if ordinal == 1:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_int32(off),
                4,
            )
            return cls(status=val)
        if ordinal == 2:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_array(
                    off, lambda dec, off: dec.read_int32(off), 4, 10
                ),
                40,
            )
            return cls(status_array=val)
        if ordinal == 3:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_vector(
                    off, lambda dec, off: dec.read_int32(off), 4, nullable=False
                ),
                16,
            )
            return cls(status_vector=val)

        # Skip unknown envelope
        decoder.skip_envelope(_offset + 8)
        raise ValueError(f"Unknown union ordinal {ordinal}")

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


class StatusEnum(enum.IntFlag):
    OK = 0

    @property
    def name(self) -> str:
        name = super().name
        return name if name is not None else f"UNKNOWN_{int(self)}"

    __fidl_kind__ = "enum"
    __fidl_type__ = "StatusEnum"
    __fidl_raw_type__ = "test.zxtypes/StatusEnum"
    __strict__ = False
    __fidl_inline_size__ = 4

    def _encode(self: int, enc: fidl.Encoder, off: int) -> None:
        enc.write_int32(int(self), off)

    @classmethod
    def _decode(cls, dec: fidl.Decoder, off: int) -> typing.Self:
        return cls(dec.read_int32(off))

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(0)


class StatusEnumAlias(enum.IntFlag):
    OK = 0

    @property
    def name(self) -> str:
        name = super().name
        return name if name is not None else f"UNKNOWN_{int(self)}"

    __fidl_kind__ = "enum"
    __fidl_type__ = "StatusEnumAlias"
    __fidl_raw_type__ = "test.zxtypes/StatusEnumAlias"
    __strict__ = False
    __fidl_inline_size__ = 4

    def _encode(self: int, enc: fidl.Encoder, off: int) -> None:
        enc.write_int32(int(self), off)

    @classmethod
    def _decode(cls, dec: fidl.Decoder, off: int) -> typing.Self:
        return cls(dec.read_int32(off))

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(0)


# Generated aliases
StatusAlias = int


class StatusProtocolServer(ServerBase, ABC):
    __fidl_kind__ = "server"
    library = "test.zxtypes"
    method_map: typing.Dict[int, MethodInfo] = {
        910477041687900864: MethodInfo(
            name="a",
            request_ident="test.zxtypes/StatusProtocolARequest",
            requires_response=True,
            empty_response=False,
            has_result=True,
            response_identifier="test.zxtypes/StatusProtocol_A_Result",
        ),
        1940401666492920560: MethodInfo(
            name="b",
            request_ident="test.zxtypes/StatusProtocolBRequest",
            requires_response=True,
            empty_response=False,
            has_result=True,
            response_identifier="test.zxtypes/StatusProtocol_B_Result",
        ),
    }

    AResponse: typing.TypeAlias = typing.Union[
        FrameworkError,
        DomainError,
        StatusProtocolAResponse,
    ]

    @abstractmethod
    def a(
        self,
        request: StatusProtocolARequest,
    ) -> (
        collections.abc.Coroutine[typing.Any, typing.Any, AResponse] | AResponse
    ):
        ...

    BResponse: typing.TypeAlias = typing.Union[
        FrameworkError,
        DomainError,
        StatusProtocolBResponse,
    ]

    @abstractmethod
    def b(
        self,
        request: StatusProtocolBRequest,
    ) -> (
        collections.abc.Coroutine[typing.Any, typing.Any, BResponse] | BResponse
    ):
        ...


class StatusProtocolClient(FidlClient):
    __fidl_kind__ = "client"
    library = "test.zxtypes"

    def a(
        self,
        *,
        arg: int,
    ) -> collections.abc.Coroutine[
        typing.Any, typing.Any, StatusProtocolAResult
    ]:
        msg = StatusProtocolARequest(
            arg,
        )
        ret = typing.cast(
            collections.abc.Coroutine[
                typing.Any, typing.Any, StatusProtocolAResult
            ],
            self._send_two_way_fidl_request(
                910477041687900864,
                "test.zxtypes",
                msg,
            ),
        )
        return ret

    def b(
        self,
        *,
        arg: int,
    ) -> collections.abc.Coroutine[
        typing.Any, typing.Any, StatusProtocolBResult
    ]:
        msg = StatusProtocolBRequest(
            arg,
        )
        ret = typing.cast(
            collections.abc.Coroutine[
                typing.Any, typing.Any, StatusProtocolBResult
            ],
            self._send_two_way_fidl_request(
                1940401666492920560,
                "test.zxtypes",
                msg,
            ),
        )
        return ret


class StatusProtocolEventHandler(EventHandlerBase, ABC):
    __fidl_kind__ = "event_handler"
    library = "test.zxtypes"
    method_map: typing.Dict[int, MethodInfo] = {}


StatusProtocolMarker = FidlProtocolMarker(
    "(nondiscoverable) test.zxtypes/StatusProtocol"
)


fidl.register_method(
    910477041687900864,
    StatusProtocolARequest,
    StatusProtocolAResult,
)
fidl.register_method(
    1940401666492920560,
    StatusProtocolBRequest,
    StatusProtocolBResult,
)
