# WARNING: This file is machine generated by fidlgen.

from __future__ import annotations

from dataclasses import dataclass
import sys
import typing
import enum

import fidl


from fidl._client import EventHandlerBase, FidlClient  # type: ignore[unused-ignore,unused-import]
from fidl._server import ServerBase  # type: ignore[unused-ignore,unused-import]
from fidl._fidl_common import DomainError, FrameworkError, MethodInfo, FidlProtocolMarker, normalize_identifier  # type: ignore[unused-ignore,unused-import]

from abc import abstractmethod, ABC  # type: ignore[unused-ignore,unused-import]
import collections.abc  # type: ignore[unused-ignore,unused-import]


_UNDEFINED: typing.Any = object()


@dataclass
class OverrideTest:
    op: int | Op
    left: typing.Optional[Expression]
    right: typing.Optional[Expression]

    __fidl_kind__ = "struct"
    __fidl_type__ = "OverrideTest"
    __fidl_raw_type__ = "test.anonymous/OverrideTest"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 40)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 40)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        Op._encode(self.op, encoder, _offset + 0)
        if self.left is not None:
            Expression._encode(self.left, encoder, _offset + 8)
        else:
            encoder.write_inline(_offset + 8, b"\x00" * 16)
        if self.right is not None:
            Expression._encode(self.right, encoder, _offset + 24)
        else:
            encoder.write_inline(_offset + 24, b"\x00" * 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        op = Op._decode(decoder, _offset + 0)
        left = (
            None
            if decoder.read_uint64(_offset + 8) == 0
            else Expression._decode(decoder, _offset + 8)
        )
        right = (
            None
            if decoder.read_uint64(_offset + 24) == 0
            else Expression._decode(decoder, _offset + 24)
        )
        return cls(
            op=op,
            left=left,
            right=right,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "op": None,  # type: ignore[arg-type,unused-ignore]
                "left": None,  # type: ignore[arg-type,unused-ignore]
                "right": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class SomeProtocolSomeMethodRequest:
    union_member: UnionMember
    table_member: TableMember

    __fidl_kind__ = "struct"
    __fidl_type__ = "SomeProtocolSomeMethodRequest"
    __fidl_raw_type__ = "test.anonymous/SomeProtocolSomeMethodRequest"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 32)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 32)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        UnionMember._encode(self.union_member, encoder, _offset + 0)
        TableMember._encode(self.table_member, encoder, _offset + 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        union_member = UnionMember._decode(decoder, _offset + 0)
        table_member = TableMember._decode(decoder, _offset + 16)
        return cls(
            union_member=union_member,
            table_member=table_member,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "union_member": None,  # type: ignore[arg-type,unused-ignore]
                "table_member": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class SomeProtocolSomeMethodResponse:
    bits_member: int | BitsMember

    __fidl_kind__ = "struct"
    __fidl_type__ = "SomeProtocolSomeMethodResponse"
    __fidl_raw_type__ = "test.anonymous/SomeProtocol_SomeMethod_Response"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 4)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 4)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        BitsMember._encode(self.bits_member, encoder, _offset + 0)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        bits_member = BitsMember._decode(decoder, _offset + 0)
        return cls(
            bits_member=bits_member,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "bits_member": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class TableData:
    data: int

    __fidl_kind__ = "struct"
    __fidl_type__ = "TableData"
    __fidl_raw_type__ = "test.anonymous/TableData"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 1)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 1)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        encoder.write_uint8(self.data, _offset + 0)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        data = decoder.read_uint8(_offset + 0)
        return cls(
            data=data,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "data": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class FunctionApplication:
    func: str | None
    args: typing.Sequence[typing.Optional[Expression]] | None
    flags: int | Flags | None

    def __init__(
        self,
        func: str | None = None,
        args: typing.Sequence[typing.Optional[Expression]] | None = None,
        flags: int | Flags | None = None,
    ) -> None:
        self.func = func
        self.args = args
        self.flags = flags

    __fidl_kind__ = "table"
    __fidl_type__ = "FunctionApplication"
    __fidl_raw_type__ = "test.anonymous/FunctionApplication"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, offset: int) -> None:
        max_ord = 0
        if self.func is not None:
            max_ord = max(max_ord, 1)
        if self.args is not None:
            max_ord = max(max_ord, 3)
        if self.flags is not None:
            max_ord = max(max_ord, 4)

        if max_ord == 0:
            encoder.write_inline(offset, b"\x00" * 8 + b"\xff" * 8)
            return

        encoder.write_uint64(max_ord, offset)
        encoder.write_uint64(0xFFFFFFFFFFFFFFFF, offset + 8)

        def encode_envelopes(
            val: typing.Any, enc: fidl.Encoder, start_offset: int
        ) -> None:
            if max_ord >= 1:
                enc.write_envelope(
                    val.func,
                    start_offset + 0,
                    lambda val, enc, off: enc.write_string(val, off),
                    16,
                    False,
                )
            if max_ord >= 2:
                enc.write_inline(start_offset + 8, b"\x00" * 8)
            if max_ord >= 3:
                enc.write_envelope(
                    val.args,
                    start_offset + 16,
                    lambda val, enc, off: enc.write_vector(
                        val,
                        off,
                        lambda val, enc, off: Expression._encode(val, enc, off)
                        if val is not None
                        else enc.write_inline(off, b"\x00" * 16),
                        16,
                    ),
                    16,
                    False,
                )
            if max_ord >= 4:
                enc.write_envelope(
                    val.flags, start_offset + 24, Flags._encode, 2, True
                )

        encoder.ool_tasks.append(
            lambda: encoder.write_ool_struct(
                self, encode_envelopes, max_ord * 8
            )
        )

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        _max_ord = decoder.read_uint64(_offset)
        _presence = decoder.read_uint64(_offset + 8)
        if _presence == 0:
            return cls()

        _pad_mod = decoder.ool_offset % 8
        if _pad_mod != 0:
            decoder.ool_offset += 8 - _pad_mod
        _env_array_start = decoder.ool_offset
        decoder.ool_offset += _max_ord * 8

        func = None
        args = None
        flags = None
        for _ord_idx in range(1, _max_ord + 1):
            _env_offset = _env_array_start + (_ord_idx - 1) * 8
            if _ord_idx == 1:
                func = decoder.read_envelope(
                    _env_offset,
                    lambda dec, off: dec.read_string(off, nullable=False),
                    16,
                )
            elif _ord_idx == 3:
                args = decoder.read_envelope(
                    _env_offset,
                    lambda dec, off: dec.read_vector(
                        off,
                        lambda dec, off: None
                        if dec.read_uint64(off) == 0
                        else Expression._decode(dec, off),
                        16,
                        nullable=False,
                    ),
                    16,
                )
            elif _ord_idx == 4:
                flags = decoder.read_envelope(_env_offset, Flags._decode, 2)
            else:
                decoder.skip_envelope(_env_offset)

        return cls(
            func=func,
            args=args,
            flags=flags,
        )

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls()


@dataclass
class TableMember:
    table_data: typing.Sequence[TableData] | None

    def __init__(
        self,
        table_data: typing.Sequence[TableData] | None = None,
    ) -> None:
        self.table_data = table_data

    __fidl_kind__ = "table"
    __fidl_type__ = "TableMember"
    __fidl_raw_type__ = "test.anonymous/TableMember"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, offset: int) -> None:
        max_ord = 0
        if self.table_data is not None:
            max_ord = max(max_ord, 2)

        if max_ord == 0:
            encoder.write_inline(offset, b"\x00" * 8 + b"\xff" * 8)
            return

        encoder.write_uint64(max_ord, offset)
        encoder.write_uint64(0xFFFFFFFFFFFFFFFF, offset + 8)

        def encode_envelopes(
            val: typing.Any, enc: fidl.Encoder, start_offset: int
        ) -> None:
            if max_ord >= 1:
                enc.write_inline(start_offset + 0, b"\x00" * 8)
            if max_ord >= 2:
                enc.write_envelope(
                    val.table_data,
                    start_offset + 8,
                    lambda val, enc, off: enc.write_vector(
                        val, off, TableData._encode, 1
                    ),
                    16,
                    False,
                )

        encoder.ool_tasks.append(
            lambda: encoder.write_ool_struct(
                self, encode_envelopes, max_ord * 8
            )
        )

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        _max_ord = decoder.read_uint64(_offset)
        _presence = decoder.read_uint64(_offset + 8)
        if _presence == 0:
            return cls()

        _pad_mod = decoder.ool_offset % 8
        if _pad_mod != 0:
            decoder.ool_offset += 8 - _pad_mod
        _env_array_start = decoder.ool_offset
        decoder.ool_offset += _max_ord * 8

        table_data = None
        for _ord_idx in range(1, _max_ord + 1):
            _env_offset = _env_array_start + (_ord_idx - 1) * 8
            if _ord_idx == 2:
                table_data = decoder.read_envelope(
                    _env_offset,
                    lambda dec, off: dec.read_vector(
                        off, TableData._decode, 1, nullable=False
                    ),
                    16,
                )
            else:
                decoder.skip_envelope(_env_offset)

        return cls(
            table_data=table_data,
        )

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls()


class Expression:
    _value: typing.Optional[int]
    _bin_op: typing.Optional[OverrideTest]
    _function_application: typing.Optional[FunctionApplication]

    __fidl_kind__ = "union"
    __fidl_type__ = "Expression"
    __fidl_raw_type__ = "test.anonymous/Expression"
    _is_result = False

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.value != other.value:
            return False
        if self.bin_op != other.bin_op:
            return False
        if self.function_application != other.function_application:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.value:
            variant = f"value={self.value!r}"
        if self.bin_op:
            variant = f"bin_op={self.bin_op!r}"
        if self.function_application:
            variant = f"function_application={self.function_application!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        value: typing.Optional[int] = _UNDEFINED,
        bin_op: typing.Optional[OverrideTest] = _UNDEFINED,
        function_application: typing.Optional[FunctionApplication] = _UNDEFINED,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if value is not _UNDEFINED:
            if value is not None:
                self._value = value
                variants.append("value")
                number_of_variants += 1
        if bin_op is not _UNDEFINED:
            if bin_op is not None:
                self._bin_op = bin_op
                variants.append("bin_op")
                number_of_variants += 1
        if function_application is not _UNDEFINED:
            if function_application is not None:
                self._function_application = function_application
                variants.append("function_application")
                number_of_variants += 1
        if number_of_variants != 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def value(self) -> int | None:
        return getattr(self, "_value", None)

    @property
    def bin_op(self) -> OverrideTest | None:
        return getattr(self, "_bin_op", None)

    @property
    def function_application(self) -> FunctionApplication | None:
        return getattr(self, "_function_application", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        if hasattr(self, "_value"):
            encoder.write_uint64(1, _offset)
            encoder.write_envelope(
                getattr(self, "_value"),
                _offset + 8,
                lambda val, enc, off: enc.write_uint64(val, off),
                8,
                False,
            )
            return
        if hasattr(self, "_bin_op"):
            encoder.write_uint64(2, _offset)
            encoder.write_envelope(
                getattr(self, "_bin_op"),
                _offset + 8,
                OverrideTest._encode,
                40,
                False,
            )
            return
        if hasattr(self, "_function_application"):
            encoder.write_uint64(3, _offset)
            encoder.write_envelope(
                getattr(self, "_function_application"),
                _offset + 8,
                FunctionApplication._encode,
                16,
                False,
            )
            return
        encoder.write_inline(_offset, b"\x00" * 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        ordinal = decoder.read_uint64(_offset)
        if ordinal == 0:
            return cls(_empty=())
        if ordinal == 1:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_uint64(off),
                8,
            )
            return cls(value=val)
        if ordinal == 2:
            val = decoder.read_envelope(
                _offset + 8,
                OverrideTest._decode,
                40,
            )
            return cls(bin_op=val)
        if ordinal == 3:
            val = decoder.read_envelope(
                _offset + 8,
                FunctionApplication._decode,
                16,
            )
            return cls(function_application=val)

        # Skip unknown envelope
        decoder.skip_envelope(_offset + 8)
        raise ValueError(f"Unknown union ordinal {ordinal}")

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


class SomeProtocolSomeMethodResult:
    _response: typing.Optional[SomeProtocolSomeMethodResponse]
    _err: typing.Optional[int | SomeProtocolSomeMethodError]

    __fidl_kind__ = "union"
    __fidl_type__ = "SomeProtocolSomeMethodResult"
    __fidl_raw_type__ = "test.anonymous/SomeProtocol_SomeMethod_Result"
    _is_result = True

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.response != other.response:
            return False
        if self.err != other.err:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.response:
            variant = f"response={self.response!r}"
        if self.err:
            variant = f"err={self.err!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        response: typing.Optional[SomeProtocolSomeMethodResponse] = _UNDEFINED,
        err: typing.Optional[int | SomeProtocolSomeMethodError] = _UNDEFINED,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if response is not _UNDEFINED:
            if response is not None:
                self._response = response
                variants.append("response")
                number_of_variants += 1
        if err is not _UNDEFINED:
            if err is not None:
                self._err = err
                variants.append("err")
                number_of_variants += 1
        if number_of_variants > 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def response(self) -> SomeProtocolSomeMethodResponse | None:
        return getattr(self, "_response", None)

    @property
    def err(self) -> int | SomeProtocolSomeMethodError | None:
        return getattr(self, "_err", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        if hasattr(self, "_response"):
            encoder.write_uint64(1, _offset)
            encoder.write_envelope(
                getattr(self, "_response"),
                _offset + 8,
                SomeProtocolSomeMethodResponse._encode,
                4,
                True,
            )
            return
        if hasattr(self, "_err"):
            encoder.write_uint64(2, _offset)
            encoder.write_envelope(
                getattr(self, "_err"),
                _offset + 8,
                SomeProtocolSomeMethodError._encode,
                4,
                True,
            )
            return
        encoder.write_inline(_offset, b"\x00" * 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        ordinal = decoder.read_uint64(_offset)
        if ordinal == 0:
            return cls(_empty=())
        if ordinal == 1:
            val = decoder.read_envelope(
                _offset + 8,
                SomeProtocolSomeMethodResponse._decode,
                4,
            )
            return cls(response=val)
        if ordinal == 2:
            val = decoder.read_envelope(
                _offset + 8,
                SomeProtocolSomeMethodError._decode,
                4,
            )
            return cls(err=val)

        # Skip unknown envelope
        decoder.skip_envelope(_offset + 8)
        raise ValueError(f"Unknown union ordinal {ordinal}")

    def unwrap(self) -> SomeProtocolSomeMethodResponse:
        """Returns the response if result does not contain an error. Otherwise, raises an exception."""
        try:
            if (
                hasattr(self, "_framework_err")
                and self._framework_err is not None
            ):
                raise AssertionError(
                    f"{self.__fidl_raw_type__} framework error {self._framework_err}"
                )
        except AttributeError:
            pass
        try:
            if hasattr(self, "_err") and self._err is not None:
                raise AssertionError(
                    f"{self.__fidl_raw_type__} error {self._err}"
                )
        except AttributeError:
            pass

        assert (
            self._response is not None
        ), f"Failed to unwrap {self.__fidl_raw_type__}. Result does not contain an error or response."
        return self._response

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


class UnionMember:
    _union_data: typing.Optional[int]

    __fidl_kind__ = "union"
    __fidl_type__ = "UnionMember"
    __fidl_raw_type__ = "test.anonymous/UnionMember"
    _is_result = False

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.union_data != other.union_data:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.union_data:
            variant = f"union_data={self.union_data!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        union_data: typing.Optional[int] = _UNDEFINED,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if union_data is not _UNDEFINED:
            if union_data is not None:
                self._union_data = union_data
                variants.append("union_data")
                number_of_variants += 1
        if number_of_variants != 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def union_data(self) -> int | None:
        return getattr(self, "_union_data", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        if hasattr(self, "_union_data"):
            encoder.write_uint64(2, _offset)
            encoder.write_envelope(
                getattr(self, "_union_data"),
                _offset + 8,
                lambda val, enc, off: enc.write_uint8(val, off),
                1,
                True,
            )
            return
        encoder.write_inline(_offset, b"\x00" * 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        ordinal = decoder.read_uint64(_offset)
        if ordinal == 0:
            return cls(_empty=())
        if ordinal == 2:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_uint8(off),
                1,
            )
            return cls(union_data=val)

        # Skip unknown envelope
        decoder.skip_envelope(_offset + 8)
        raise ValueError(f"Unknown union ordinal {ordinal}")

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


class Op(enum.IntFlag):
    ADD = 1
    MUL = 2
    DIV = 3
    EMPTY__ = 0

    @property
    def name(self) -> str:
        name = super().name
        return name if name is not None else f"UNKNOWN_{int(self)}"

    __fidl_kind__ = "enum"
    __fidl_type__ = "Op"
    __fidl_raw_type__ = "test.anonymous/Op"
    __strict__ = False
    __fidl_inline_size__ = 4

    def _encode(self: int, enc: fidl.Encoder, off: int) -> None:
        enc.write_uint32(int(self), off)

    @classmethod
    def _decode(cls, dec: fidl.Decoder, off: int) -> typing.Self:
        return cls(dec.read_uint32(off))

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(0)


class SomeProtocolSomeMethodError(enum.IntFlag):
    ERROR_ONE = 1
    ERROR_TWO = 2
    EMPTY__ = 0

    @property
    def name(self) -> str:
        name = super().name
        return name if name is not None else f"UNKNOWN_{int(self)}"

    __fidl_kind__ = "enum"
    __fidl_type__ = "SomeProtocolSomeMethodError"
    __fidl_raw_type__ = "test.anonymous/SomeProtocol_SomeMethod_Error"
    __strict__ = False
    __fidl_inline_size__ = 4

    def _encode(self: int, enc: fidl.Encoder, off: int) -> None:
        enc.write_uint32(int(self), off)

    @classmethod
    def _decode(cls, dec: fidl.Decoder, off: int) -> typing.Self:
        return cls(dec.read_uint32(off))

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(0)


class BitsMember(enum.IntFlag):
    BIT_ONE = 1
    BIT_TWO = 2
    __fidl_kind__ = "bits"
    __fidl_type__ = "BitsMember"
    __fidl_raw_type__ = "test.anonymous/BitsMember"
    __strict__ = False
    __fidl_inline_size__ = 4

    def _encode(self: int, enc: fidl.Encoder, off: int) -> None:
        enc.write_uint32(int(self), off)

    @classmethod
    def _decode(cls, dec: fidl.Decoder, off: int) -> typing.Self:
        return cls(dec.read_uint32(off))

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(value=0)


class Flags(enum.IntFlag):
    INLINE = 1
    __fidl_kind__ = "bits"
    __fidl_type__ = "Flags"
    __fidl_raw_type__ = "test.anonymous/Flags"
    __strict__ = False
    __fidl_inline_size__ = 2

    def _encode(self: int, enc: fidl.Encoder, off: int) -> None:
        enc.write_uint16(int(self), off)

    @classmethod
    def _decode(cls, dec: fidl.Decoder, off: int) -> typing.Self:
        return cls(dec.read_uint16(off))

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(value=0)


class SomeProtocolServer(ServerBase, ABC):
    __fidl_kind__ = "server"
    library = "test.anonymous"
    method_map: typing.Dict[int, MethodInfo] = {
        2515914517457749720: MethodInfo(
            name="some_method",
            request_ident="test.anonymous/SomeProtocolSomeMethodRequest",
            requires_response=True,
            empty_response=False,
            has_result=True,
            response_identifier="test.anonymous/SomeProtocol_SomeMethod_Result",
        ),
    }

    SomeMethodResponse: typing.TypeAlias = typing.Union[
        DomainError,
        SomeProtocolSomeMethodResponse,
    ]

    @abstractmethod
    def some_method(
        self,
        request: SomeProtocolSomeMethodRequest,
    ) -> (
        collections.abc.Coroutine[typing.Any, typing.Any, SomeMethodResponse]
        | SomeMethodResponse
    ):
        ...


class SomeProtocolClient(FidlClient):
    __fidl_kind__ = "client"
    library = "test.anonymous"

    def some_method(
        self,
        *,
        union_member: UnionMember,
        table_member: TableMember,
    ) -> collections.abc.Coroutine[
        typing.Any, typing.Any, SomeProtocolSomeMethodResult
    ]:
        msg = SomeProtocolSomeMethodRequest(
            union_member,
            table_member,
        )
        ret = typing.cast(
            collections.abc.Coroutine[
                typing.Any, typing.Any, SomeProtocolSomeMethodResult
            ],
            self._send_two_way_fidl_request(
                2515914517457749720,
                "test.anonymous",
                msg,
            ),
        )
        return ret


class SomeProtocolEventHandler(EventHandlerBase, ABC):
    __fidl_kind__ = "event_handler"
    library = "test.anonymous"
    method_map: typing.Dict[int, MethodInfo] = {}


SomeProtocolMarker = FidlProtocolMarker(
    "(nondiscoverable) test.anonymous/SomeProtocol"
)


fidl.register_method(
    2515914517457749720,
    SomeProtocolSomeMethodRequest,
    SomeProtocolSomeMethodResult,
)
