# WARNING: This file is machine generated by fidlgen.

from __future__ import annotations

from dataclasses import dataclass
import sys
import typing
import enum

import fidl


from fidl._client import EventHandlerBase, FidlClient  # type: ignore[unused-ignore,unused-import]
from fidl._server import ServerBase  # type: ignore[unused-ignore,unused-import]
from fidl._fidl_common import DomainError, FrameworkError, MethodInfo, FidlProtocolMarker, normalize_identifier  # type: ignore[unused-ignore,unused-import]

from abc import abstractmethod, ABC  # type: ignore[unused-ignore,unused-import]
import collections.abc  # type: ignore[unused-ignore,unused-import]


_UNDEFINED: typing.Any = object()


@dataclass
class EmptyResourceStruct:
    __fidl_kind__ = "struct"
    __fidl_type__ = "EmptyResourceStruct"
    __fidl_raw_type__ = "test.handlesintypes/EmptyResourceStruct"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 1)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 1)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        pass

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        return cls()

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(**{})


@dataclass
class HandlesInTypes:
    normal_handle: int
    handle_in_vec: typing.Sequence[int]
    handle_in_array: typing.Sequence[int]
    handle_in_mixed_vec_array: typing.Sequence[typing.Sequence[int]]
    table_with_handle: TableWithHandle
    union_with_handle: UnionWithHandle

    __fidl_kind__ = "struct"
    __fidl_type__ = "HandlesInTypes"
    __fidl_raw_type__ = "test.handlesintypes/HandlesInTypes"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 96)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 96)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        encoder.write_handle(
            self.normal_handle, _offset + 0, obj_type=3, rights=2147483648
        )
        encoder.write_vector(
            self.handle_in_vec,
            _offset + 8,
            lambda val, enc, off: enc.write_handle(
                val, off, obj_type=3, rights=2147483648
            ),
            4,
        )
        encoder.write_array(
            self.handle_in_array,
            _offset + 24,
            lambda val, enc, off: enc.write_handle(
                val, off, obj_type=3, rights=2147483648
            ),
            4,
            5,
        )
        encoder.write_vector(
            self.handle_in_mixed_vec_array,
            _offset + 48,
            lambda val, enc, off: enc.write_array(
                val,
                off,
                lambda val, enc, off: enc.write_handle(
                    val, off, obj_type=3, rights=2147483648
                ),
                4,
                5,
            ),
            20,
        )
        TableWithHandle._encode(self.table_with_handle, encoder, _offset + 64)
        UnionWithHandle._encode(self.union_with_handle, encoder, _offset + 80)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        normal_handle = decoder.read_handle(_offset + 0, nullable=False)
        handle_in_vec = decoder.read_vector(
            _offset + 8,
            lambda dec, off: dec.read_handle(off, nullable=False),
            4,
            nullable=False,
        )
        handle_in_array = decoder.read_array(
            _offset + 24,
            lambda dec, off: dec.read_handle(off, nullable=False),
            4,
            5,
        )
        handle_in_mixed_vec_array = decoder.read_vector(
            _offset + 48,
            lambda dec, off: dec.read_array(
                off, lambda dec, off: dec.read_handle(off, nullable=False), 4, 5
            ),
            20,
            nullable=False,
        )
        table_with_handle = TableWithHandle._decode(decoder, _offset + 64)
        union_with_handle = UnionWithHandle._decode(decoder, _offset + 80)
        return cls(
            normal_handle=normal_handle,
            handle_in_vec=handle_in_vec,
            handle_in_array=handle_in_array,
            handle_in_mixed_vec_array=handle_in_mixed_vec_array,
            table_with_handle=table_with_handle,
            union_with_handle=union_with_handle,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "normal_handle": None,  # type: ignore[arg-type,unused-ignore]
                "handle_in_vec": None,  # type: ignore[arg-type,unused-ignore]
                "handle_in_array": None,  # type: ignore[arg-type,unused-ignore]
                "handle_in_mixed_vec_array": None,  # type: ignore[arg-type,unused-ignore]
                "table_with_handle": None,  # type: ignore[arg-type,unused-ignore]
                "union_with_handle": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class EmptyResourceTable:
    __fidl_kind__ = "table"
    __fidl_type__ = "EmptyResourceTable"
    __fidl_raw_type__ = "test.handlesintypes/EmptyResourceTable"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, offset: int) -> None:
        max_ord = 0

        if max_ord == 0:
            encoder.write_inline(offset, b"\x00" * 8 + b"\xff" * 8)
            return

        encoder.write_uint64(max_ord, offset)
        encoder.write_uint64(0xFFFFFFFFFFFFFFFF, offset + 8)

        def encode_envelopes(
            val: typing.Any, enc: fidl.Encoder, start_offset: int
        ) -> None:
            pass

        encoder.ool_tasks.append(
            lambda: encoder.write_ool_struct(
                self, encode_envelopes, max_ord * 8
            )
        )

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        _max_ord = decoder.read_uint64(_offset)
        _presence = decoder.read_uint64(_offset + 8)
        if _presence == 0:
            return cls()

        _pad_mod = decoder.ool_offset % 8
        if _pad_mod != 0:
            decoder.ool_offset += 8 - _pad_mod
        _env_array_start = decoder.ool_offset
        decoder.ool_offset += _max_ord * 8

        for _ord_idx in range(1, _max_ord + 1):
            _env_offset = _env_array_start + (_ord_idx - 1) * 8
            decoder.skip_envelope(_env_offset)

        return cls()

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls()


@dataclass
class TableWithHandle:
    h: int | None

    def __init__(
        self,
        h: int | None = None,
    ) -> None:
        self.h = h

    __fidl_kind__ = "table"
    __fidl_type__ = "TableWithHandle"
    __fidl_raw_type__ = "test.handlesintypes/TableWithHandle"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, offset: int) -> None:
        max_ord = 0
        if self.h is not None:
            max_ord = max(max_ord, 1)

        if max_ord == 0:
            encoder.write_inline(offset, b"\x00" * 8 + b"\xff" * 8)
            return

        encoder.write_uint64(max_ord, offset)
        encoder.write_uint64(0xFFFFFFFFFFFFFFFF, offset + 8)

        def encode_envelopes(
            val: typing.Any, enc: fidl.Encoder, start_offset: int
        ) -> None:
            if max_ord >= 1:
                enc.write_envelope(
                    val.h,
                    start_offset + 0,
                    lambda val, enc, off: enc.write_handle(
                        val, off, obj_type=3, rights=2147483648
                    ),
                    4,
                    True,
                )

        encoder.ool_tasks.append(
            lambda: encoder.write_ool_struct(
                self, encode_envelopes, max_ord * 8
            )
        )

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        _max_ord = decoder.read_uint64(_offset)
        _presence = decoder.read_uint64(_offset + 8)
        if _presence == 0:
            return cls()

        _pad_mod = decoder.ool_offset % 8
        if _pad_mod != 0:
            decoder.ool_offset += 8 - _pad_mod
        _env_array_start = decoder.ool_offset
        decoder.ool_offset += _max_ord * 8

        h = None
        for _ord_idx in range(1, _max_ord + 1):
            _env_offset = _env_array_start + (_ord_idx - 1) * 8
            if _ord_idx == 1:
                h = decoder.read_envelope(
                    _env_offset,
                    lambda dec, off: dec.read_handle(off, nullable=False),
                    4,
                )
            else:
                decoder.skip_envelope(_env_offset)

        return cls(
            h=h,
        )

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls()


class EmptyResourceUnion:
    __fidl_kind__ = "union"
    __fidl_type__ = "EmptyResourceUnion"
    __fidl_raw_type__ = "test.handlesintypes/EmptyResourceUnion"
    _is_result = False

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        return f"<'{self.__fidl_type__}' object({variant})>"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        encoder.write_inline(_offset, b"\x00" * 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        ordinal = decoder.read_uint64(_offset)
        if ordinal == 0:
            return cls()

        # Skip unknown envelope
        decoder.skip_envelope(_offset + 8)
        raise ValueError(f"Unknown union ordinal {ordinal}")

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls()


class UnionWithHandle:
    _h: typing.Optional[int]

    __fidl_kind__ = "union"
    __fidl_type__ = "UnionWithHandle"
    __fidl_raw_type__ = "test.handlesintypes/UnionWithHandle"
    _is_result = False

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.h != other.h:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.h:
            variant = f"h={self.h!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        h: typing.Optional[int] = _UNDEFINED,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if h is not _UNDEFINED:
            if h is not None:
                self._h = h
                variants.append("h")
                number_of_variants += 1
        if number_of_variants != 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def h(self) -> int | None:
        return getattr(self, "_h", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        if hasattr(self, "_h"):
            encoder.write_uint64(1, _offset)
            encoder.write_envelope(
                getattr(self, "_h"),
                _offset + 8,
                lambda val, enc, off: enc.write_handle(
                    val, off, obj_type=3, rights=2147483648
                ),
                4,
                True,
            )
            return
        encoder.write_inline(_offset, b"\x00" * 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        ordinal = decoder.read_uint64(_offset)
        if ordinal == 0:
            return cls(_empty=())
        if ordinal == 1:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_handle(off, nullable=False),
                4,
            )
            return cls(h=val)

        # Skip unknown envelope
        decoder.skip_envelope(_offset + 8)
        raise ValueError(f"Unknown union ordinal {ordinal}")

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


class ObjType(enum.IntEnum):
    NONE = 0
    VMO = 3

    __fidl_kind__ = "enum"
    __fidl_type__ = "ObjType"
    __fidl_raw_type__ = "test.handlesintypes/obj_type"
    __strict__ = True
    __fidl_inline_size__ = 4

    def _encode(self: int, enc: fidl.Encoder, off: int) -> None:
        enc.write_uint32(int(self), off)

    @classmethod
    def _decode(cls, dec: fidl.Decoder, off: int) -> typing.Self:
        return cls(dec.read_uint32(off))

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(0)
