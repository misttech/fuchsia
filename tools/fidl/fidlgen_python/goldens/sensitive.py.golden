# WARNING: This file is machine generated by fidlgen.

from __future__ import annotations

from dataclasses import dataclass
import sys
import typing
import enum

import fidl


from fidl._client import EventHandlerBase, FidlClient  # type: ignore[unused-ignore,unused-import]
from fidl._server import ServerBase  # type: ignore[unused-ignore,unused-import]
from fidl._fidl_common import DomainError, FrameworkError, MethodInfo, FidlProtocolMarker, normalize_identifier  # type: ignore[unused-ignore,unused-import]

from abc import abstractmethod, ABC  # type: ignore[unused-ignore,unused-import]
import collections.abc  # type: ignore[unused-ignore,unused-import]


_UNDEFINED: typing.Any = object()


@dataclass
class ContainerWithPropagation:
    unannotated_field: str
    sensitive_struct_field: SensitiveStruct
    sensitive_table_field: SensitiveTable
    sensitive_union_field: SensitiveUnion

    __fidl_kind__ = "struct"
    __fidl_type__ = "ContainerWithPropagation"
    __fidl_raw_type__ = "test.sensitive/ContainerWithPropagation"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 64)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 64)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        encoder.write_string(self.unannotated_field, _offset + 0)
        SensitiveStruct._encode(
            self.sensitive_struct_field, encoder, _offset + 16
        )
        SensitiveTable._encode(
            self.sensitive_table_field, encoder, _offset + 32
        )
        SensitiveUnion._encode(
            self.sensitive_union_field, encoder, _offset + 48
        )

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        unannotated_field = decoder.read_string(_offset + 0, nullable=False)
        sensitive_struct_field = SensitiveStruct._decode(decoder, _offset + 16)
        sensitive_table_field = SensitiveTable._decode(decoder, _offset + 32)
        sensitive_union_field = SensitiveUnion._decode(decoder, _offset + 48)
        return cls(
            unannotated_field=unannotated_field,
            sensitive_struct_field=sensitive_struct_field,
            sensitive_table_field=sensitive_table_field,
            sensitive_union_field=sensitive_union_field,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "unannotated_field": None,  # type: ignore[arg-type,unused-ignore]
                "sensitive_struct_field": None,  # type: ignore[arg-type,unused-ignore]
                "sensitive_table_field": None,  # type: ignore[arg-type,unused-ignore]
                "sensitive_union_field": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class Empty:
    __fidl_kind__ = "struct"
    __fidl_type__ = "Empty"
    __fidl_raw_type__ = "test.sensitive/Empty"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 1)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 1)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        pass

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        return cls()

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(**{})


@dataclass
class NonSensitiveStruct:
    sensitive_field: str
    sensitive_struct_field: SensitiveStruct

    __fidl_kind__ = "struct"
    __fidl_type__ = "NonSensitiveStruct"
    __fidl_raw_type__ = "test.sensitive/NonSensitiveStruct"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 32)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 32)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        encoder.write_string(self.sensitive_field, _offset + 0)
        SensitiveStruct._encode(
            self.sensitive_struct_field, encoder, _offset + 16
        )

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        sensitive_field = decoder.read_string(_offset + 0, nullable=False)
        sensitive_struct_field = SensitiveStruct._decode(decoder, _offset + 16)
        return cls(
            sensitive_field=sensitive_field,
            sensitive_struct_field=sensitive_struct_field,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "sensitive_field": None,  # type: ignore[arg-type,unused-ignore]
                "sensitive_struct_field": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class SensitiveStruct:
    field: str

    __fidl_kind__ = "struct"
    __fidl_type__ = "SensitiveStruct"
    __fidl_raw_type__ = "test.sensitive/SensitiveStruct"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        encoder.write_string(self.field, _offset + 0)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        field = decoder.read_string(_offset + 0, nullable=False)
        return cls(
            field=field,
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "field": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class NonSensitiveTable:
    sensitive_field: str | None
    sensitive_struct_field: SensitiveStruct | None

    def __init__(
        self,
        sensitive_field: str | None = None,
        sensitive_struct_field: SensitiveStruct | None = None,
    ) -> None:
        self.sensitive_field = sensitive_field
        self.sensitive_struct_field = sensitive_struct_field

    __fidl_kind__ = "table"
    __fidl_type__ = "NonSensitiveTable"
    __fidl_raw_type__ = "test.sensitive/NonSensitiveTable"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, offset: int) -> None:
        max_ord = 0
        if self.sensitive_field is not None:
            max_ord = max(max_ord, 1)
        if self.sensitive_struct_field is not None:
            max_ord = max(max_ord, 2)

        if max_ord == 0:
            encoder.write_inline(offset, b"\x00" * 8 + b"\xff" * 8)
            return

        encoder.write_uint64(max_ord, offset)
        encoder.write_uint64(0xFFFFFFFFFFFFFFFF, offset + 8)

        def encode_envelopes(
            val: typing.Any, enc: fidl.Encoder, start_offset: int
        ) -> None:
            if max_ord >= 1:
                enc.write_envelope(
                    val.sensitive_field,
                    start_offset + 0,
                    lambda val, enc, off: enc.write_string(val, off),
                    16,
                    False,
                )
            if max_ord >= 2:
                enc.write_envelope(
                    val.sensitive_struct_field,
                    start_offset + 8,
                    SensitiveStruct._encode,
                    16,
                    False,
                )

        encoder.ool_tasks.append(
            lambda: encoder.write_ool_struct(
                self, encode_envelopes, max_ord * 8
            )
        )

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        _max_ord = decoder.read_uint64(_offset)
        _presence = decoder.read_uint64(_offset + 8)
        if _presence == 0:
            return cls()

        _pad_mod = decoder.ool_offset % 8
        if _pad_mod != 0:
            decoder.ool_offset += 8 - _pad_mod
        _env_array_start = decoder.ool_offset
        decoder.ool_offset += _max_ord * 8

        sensitive_field = None
        sensitive_struct_field = None
        for _ord_idx in range(1, _max_ord + 1):
            _env_offset = _env_array_start + (_ord_idx - 1) * 8
            if _ord_idx == 1:
                sensitive_field = decoder.read_envelope(
                    _env_offset,
                    lambda dec, off: dec.read_string(off, nullable=False),
                    16,
                )
            elif _ord_idx == 2:
                sensitive_struct_field = decoder.read_envelope(
                    _env_offset, SensitiveStruct._decode, 16
                )
            else:
                decoder.skip_envelope(_env_offset)

        return cls(
            sensitive_field=sensitive_field,
            sensitive_struct_field=sensitive_struct_field,
        )

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls()


@dataclass
class SensitiveTable:
    field: str | None

    def __init__(
        self,
        field: str | None = None,
    ) -> None:
        self.field = field

    __fidl_kind__ = "table"
    __fidl_type__ = "SensitiveTable"
    __fidl_raw_type__ = "test.sensitive/SensitiveTable"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, offset: int) -> None:
        max_ord = 0
        if self.field is not None:
            max_ord = max(max_ord, 1)

        if max_ord == 0:
            encoder.write_inline(offset, b"\x00" * 8 + b"\xff" * 8)
            return

        encoder.write_uint64(max_ord, offset)
        encoder.write_uint64(0xFFFFFFFFFFFFFFFF, offset + 8)

        def encode_envelopes(
            val: typing.Any, enc: fidl.Encoder, start_offset: int
        ) -> None:
            if max_ord >= 1:
                enc.write_envelope(
                    val.field,
                    start_offset + 0,
                    lambda val, enc, off: enc.write_string(val, off),
                    16,
                    False,
                )

        encoder.ool_tasks.append(
            lambda: encoder.write_ool_struct(
                self, encode_envelopes, max_ord * 8
            )
        )

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        _max_ord = decoder.read_uint64(_offset)
        _presence = decoder.read_uint64(_offset + 8)
        if _presence == 0:
            return cls()

        _pad_mod = decoder.ool_offset % 8
        if _pad_mod != 0:
            decoder.ool_offset += 8 - _pad_mod
        _env_array_start = decoder.ool_offset
        decoder.ool_offset += _max_ord * 8

        field = None
        for _ord_idx in range(1, _max_ord + 1):
            _env_offset = _env_array_start + (_ord_idx - 1) * 8
            if _ord_idx == 1:
                field = decoder.read_envelope(
                    _env_offset,
                    lambda dec, off: dec.read_string(off, nullable=False),
                    16,
                )
            else:
                decoder.skip_envelope(_env_offset)

        return cls(
            field=field,
        )

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls()


class NonSensitiveUnion:
    _sensitive_field: typing.Optional[str]
    _sensitive_struct_field: typing.Optional[SensitiveStruct]

    __fidl_kind__ = "union"
    __fidl_type__ = "NonSensitiveUnion"
    __fidl_raw_type__ = "test.sensitive/NonSensitiveUnion"
    _is_result = False

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.sensitive_field != other.sensitive_field:
            return False
        if self.sensitive_struct_field != other.sensitive_struct_field:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.sensitive_field:
            variant = f"sensitive_field={self.sensitive_field!r}"
        if self.sensitive_struct_field:
            variant = f"sensitive_struct_field={self.sensitive_struct_field!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        sensitive_field: typing.Optional[str] = _UNDEFINED,
        sensitive_struct_field: typing.Optional[SensitiveStruct] = _UNDEFINED,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if sensitive_field is not _UNDEFINED:
            if sensitive_field is not None:
                self._sensitive_field = sensitive_field
                variants.append("sensitive_field")
                number_of_variants += 1
        if sensitive_struct_field is not _UNDEFINED:
            if sensitive_struct_field is not None:
                self._sensitive_struct_field = sensitive_struct_field
                variants.append("sensitive_struct_field")
                number_of_variants += 1
        if number_of_variants != 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def sensitive_field(self) -> str | None:
        return getattr(self, "_sensitive_field", None)

    @property
    def sensitive_struct_field(self) -> SensitiveStruct | None:
        return getattr(self, "_sensitive_struct_field", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        if hasattr(self, "_sensitive_field"):
            encoder.write_uint64(1, _offset)
            encoder.write_envelope(
                getattr(self, "_sensitive_field"),
                _offset + 8,
                lambda val, enc, off: enc.write_string(val, off),
                16,
                False,
            )
            return
        if hasattr(self, "_sensitive_struct_field"):
            encoder.write_uint64(2, _offset)
            encoder.write_envelope(
                getattr(self, "_sensitive_struct_field"),
                _offset + 8,
                SensitiveStruct._encode,
                16,
                False,
            )
            return
        encoder.write_inline(_offset, b"\x00" * 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        ordinal = decoder.read_uint64(_offset)
        if ordinal == 0:
            return cls(_empty=())
        if ordinal == 1:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_string(off, nullable=False),
                16,
            )
            return cls(sensitive_field=val)
        if ordinal == 2:
            val = decoder.read_envelope(
                _offset + 8,
                SensitiveStruct._decode,
                16,
            )
            return cls(sensitive_struct_field=val)

        # Skip unknown envelope
        decoder.skip_envelope(_offset + 8)
        raise ValueError(f"Unknown union ordinal {ordinal}")

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


class SensitiveUnion:
    _field: typing.Optional[str]

    __fidl_kind__ = "union"
    __fidl_type__ = "SensitiveUnion"
    __fidl_raw_type__ = "test.sensitive/SensitiveUnion"
    _is_result = False

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.field != other.field:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.field:
            variant = f"field={self.field!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        field: typing.Optional[str] = _UNDEFINED,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if field is not _UNDEFINED:
            if field is not None:
                self._field = field
                variants.append("field")
                number_of_variants += 1
        if number_of_variants != 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def field(self) -> str | None:
        return getattr(self, "_field", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    def encode(self) -> tuple[bytes, list[typing.Any]]:
        return fidl.encode_struct(self, type(self)._encode, 16)

    @classmethod
    def decode(cls, data: bytes, handles: list[typing.Any]) -> typing.Self:
        return fidl.decode_struct(data, handles, cls._decode, 16)

    def _encode(self, encoder: fidl.Encoder, _offset: int) -> None:
        if hasattr(self, "_field"):
            encoder.write_uint64(1, _offset)
            encoder.write_envelope(
                getattr(self, "_field"),
                _offset + 8,
                lambda val, enc, off: enc.write_string(val, off),
                16,
                False,
            )
            return
        encoder.write_inline(_offset, b"\x00" * 16)

    @classmethod
    def _decode(cls, decoder: fidl.Decoder, _offset: int) -> typing.Self:
        ordinal = decoder.read_uint64(_offset)
        if ordinal == 0:
            return cls(_empty=())
        if ordinal == 1:
            val = decoder.read_envelope(
                _offset + 8,
                lambda dec, off: dec.read_string(off, nullable=False),
                16,
            )
            return cls(field=val)

        # Skip unknown envelope
        decoder.skip_envelope(_offset + 8)
        raise ValueError(f"Unknown union ordinal {ordinal}")

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())
