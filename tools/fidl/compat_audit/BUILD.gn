# Copyright 2025 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("//build/python/python_binary.gni")

python_binary("compat_audit") {
  main_source = "compat_audit.py"
  sources = [ "compat_audit.py" ]
  deps = [ "//tools/fidl/platform-ir:platform_ir_HEAD($default_toolchain)" ]
  data_sources = [ "$root_build_dir/platform-ir-HEAD.json" ]
  data_package_name = "compat_audit_data"
}
