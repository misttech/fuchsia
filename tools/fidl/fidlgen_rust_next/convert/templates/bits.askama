pub fn {{ bits.name.decl_name().snake() }}_from_rust_next(
    value: {{ next_ty }}
) -> {{ ty }} {
    {{ ty }}::from_bits_retain(value.bits())
}

pub fn {{ bits.name.decl_name().snake() }}_into_rust_next(
    value: {{ ty }}
) -> {{ next_ty }} {
    {{ next_ty }}::from_bits_retain(value.bits())
}
