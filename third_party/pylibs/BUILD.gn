# Copyright 2026 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("//build/testing/host_test_data.gni")

assert(is_host, "pylibs only supported in host toolchains")

host_test_data("autoflake") {
  sources = [
    "autoflake/main.py",
    "autoflake/src/autoflake.py",
  ]
  deps = [ ":pyflakes" ]
}

host_test_data("isort") {
  sources = [
    "isort/main.py",
    "isort/src/isort",
  ]
}

host_test_data("pyflakes") {
  sources = [ "pyflakes/src/pyflakes" ]
}
